local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Yuna-ux/Other-scripts/refs/heads/main/UI-Library.lua"))()
local vu2 = loadstring(game:HttpGet("https://raw.githubusercontent.com/Laelmano24/brookhaven-tool/refs/heads/main/src/main.luau"))()
-- 1. SERVIÇOS E VARIÁVEIS
local vu6 = game:GetService("Players")
local vu7 = vu6.LocalPlayer
local vu8 = game:GetService("ReplicatedStorage")
local vu9 = game:GetService("Workspace")
local vu10 = game:GetService("RunService")
local vuCam = vu9.CurrentCamera

-- Variáveis de Controle
local walkSpeed, jumpPower, gravity = 16, 50, 192.2
local viewEnabled = false
_G.SelectedPlayer = ""
_G.AntiSit = false
_G.AntiVoid = false
_G.AntiFling = false 
_G.AntiLag = false 
_G.RGBName = false
_G.RainbowBio = false

-- Variáveis do Black Hole
_G.BlackHoleActive = false
_G.BH_Strength = 200
local BH_Attachment = nil
local BH_Part = nil

local toggles = { LagLaptop = false, LagPhone = false }
local BombActive = false
local loopAtivo = false
local InputID = ""

-- 2. FUNÇÕES DE SUPORTE (BACKEND)

-- Função de Câmera (CORREÇÃO PARA O VIEW FUNCIONAR)
local function updateCamera()
    pcall(function()
        if viewEnabled then
            local target = vu6:FindFirstChild(_G.SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = target.Character.Humanoid
            end
        else
            if vu7.Character and vu7.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = vu7.Character.Humanoid
            end
        end
    end)
end

-- --- FUNÇÃO ANTI-LAG ---
local function ApplyAntiLag(state)
    if state then
        game:GetService("Lighting").GlobalShadows = false
        game:GetService("Lighting").FogEnd = 9e9
        settings().Rendering.QualityLevel = 1
        for _, v in pairs(vu9:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Material = Enum.Material.SmoothPlastic
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            end
        end
    else
        game:GetService("Lighting").GlobalShadows = true
        for _, v in pairs(vu9:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Material = Enum.Material.Plastic
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 0
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = true
            end
        end
    end
end

-- --- FUNÇÕES DE SUPORTE ---
local function applyCollisionLogic(obj)
    if not obj:IsA("BasePart") then return end
    local name = obj.Name:lower()
    local isCar = false
    local parent = obj
    while parent do
        if parent.Name:lower():find(carKeyword) then isCar = true break end
        parent = parent.Parent
    end
    if isCar then obj.CanCollide = not antiCarSit end
end

local function updateSitState()
    local hum = vu7.Character and vu7.Character:FindFirstChildOfClass("Humanoid")
    if hum then
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, not (antiToolSit or antiCarSit))
        if (antiToolSit or antiCarSit) and hum.Sit then hum.Sit = false end
    end
end

-- --- FUNÇÕES HEADSIT ---
local function headsitOnPlayer(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return end
    local char = vu7.Character
    local root = char:FindFirstChild("HumanoidRootPart")
    local tHead = targetPlayer.Character:FindFirstChild("Head")
    if root and tHead then
        root.CFrame = tHead.CFrame * CFrame.new(0, 2.2, 0)
        local weld = Instance.new("WeldConstraint", root)
        weld.Part0 = root
        weld.Part1 = tHead
        char.Humanoid.Sit = true
    end
end

local function removeHeadsit()
    if vu7.Character and vu7.Character:FindFirstChild("HumanoidRootPart") then
        for _, v in pairs(vu7.Character.HumanoidRootPart:GetChildren()) do
            if v:IsA("WeldConstraint") then v:Destroy() end
        end
        vu7.Character.Humanoid.Sit = false
    end
end

-- Função para o Lag (ClickDetector)
local function clickNormally(object)
    local clickDetector = object:FindFirstChildWhichIsA("ClickDetector")
    if clickDetector then fireclickdetector(clickDetector) end
end

local function FlingBall(targetName)
    local target = game:GetService("Players"):FindFirstChild(targetName)
    if not target then return end
    local players = game:GetService("Players")
    local player = players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local backpack = player:WaitForChild("Backpack")
    local ServerBalls = workspace.WorkspaceCom:WaitForChild("001_SoccerBalls")
    local function GetBall()
        if not backpack:FindFirstChild("SoccerBall") then
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "SoccerBall")
        end
        repeat task.wait() until backpack:FindFirstChild("SoccerBall")
        backpack.SoccerBall.Parent = character
        repeat task.wait() until ServerBalls:FindFirstChild("Soccer" .. player.Name)
        character.SoccerBall.Parent = backpack
        return ServerBalls:FindFirstChild("Soccer" .. player.Name)
    end

    local Ball = ServerBalls:FindFirstChild("Soccer" .. player.Name) or GetBall()
    Ball.CanCollide = false
    Ball.Massless = true
    Ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

    if target ~= player then
        local tchar = target.Character
        if tchar and tchar:FindFirstChild("HumanoidRootPart") and tchar:FindFirstChild("Humanoid") then
            local troot = tchar.HumanoidRootPart
            local thum = tchar.Humanoid
            
            if Ball:FindFirstChildWhichIsA("BodyVelocity") then
                Ball:FindFirstChildWhichIsA("BodyVelocity"):Destroy()
            end
            
            local bv = Instance.new("BodyVelocity")
            bv.Name = "FlingPower"
            bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.P = 9e900
            bv.Parent = Ball
            
            workspace.CurrentCamera.CameraSubject = thum
            
            local connection
            connection = vu10.Heartbeat:Connect(function()
                if not tchar or not tchar:IsDescendantOf(workspace) or thum.Health <= 0 or target.Parent ~= players or troot.Velocity.Magnitude > 1000 then
                    workspace.CurrentCamera.CameraSubject = humanoid
                    connection:Disconnect()
                    return
                end
                
                if troot.Velocity.Magnitude > 0 then
                    local pos_x = troot.Position.X + (troot.Velocity.X / 1.5)
                    local pos_y = troot.Position.Y + (troot.Velocity.Y / 1.5)
                    local pos_z = troot.Position.Z + (troot.Velocity.Z / 1.5)
                    Ball.CFrame = CFrame.new(Vector3.new(pos_x, pos_y, pos_z))
                    Ball.Orientation += Vector3.new(45, 60, 30)
                else
                    for _, v in pairs(tchar:GetChildren()) do
                        if v:IsA("BasePart") and v.CanCollide and not v.Anchored then
                            Ball.CFrame = v.CFrame
                        end
                    end
                end
            end)
        end
    end
end
local function updateCamera()
    pcall(function()
        if viewEnabled then
            local target = vu6:FindFirstChild(_G.SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = target.Character.Humanoid
            end
        else
            if vu7.Character and vu7.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = vu7.Character.Humanoid
            end
        end
    end)
end

-- Configuração do Black Hole
local function SetupBlackHole()
    if not BH_Part then
        BH_Part = Instance.new("Part")
        BH_Part.Name = "BH_TargetPart"
        BH_Part.Anchored = true
        BH_Part.CanCollide = false
        BH_Part.Transparency = 1
        BH_Part.Parent = vu9
        BH_Attachment = Instance.new("Attachment", BH_Part)
    end
end

-- Network Bypass (Permite mover peças do mapa)
if not getgenv().NetworkBypassed then
    getgenv().NetworkBypassed = true
    task.spawn(function()
        while true do
            pcall(function()
                sethiddenproperty(vu7, "SimulationRadius", math.huge)
            end)
            vu10.Heartbeat:Wait()
        end
    end)
end

-- Forçar partes (Física do Black Hole)
local function ForcePart(v)
    if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and v.Name ~= "Handle" then
        for _, x in ipairs(v:GetChildren()) do
            if x:IsA("BodyMover") or x:IsA("AlignPosition") or x:IsA("Torque") then x:Destroy() end
        end
        v.CanCollide = false
        local torque = Instance.new("Torque", v)
        torque.Torque = Vector3.new(100000000, 1000000000, 100000000)
        local att0 = Instance.new("Attachment", v)
        torque.Attachment0 = att0
        local alignPos = Instance.new("AlignPosition", v)
        alignPos.Name = "BH_Force"
        alignPos.MaxForce = math.huge
        alignPos.MaxVelocity = math.huge
        alignPos.Responsiveness = _G.BH_Strength
        alignPos.Attachment0 = att0
        alignPos.Attachment1 = BH_Attachment
    end
end

-- Lógica de Pulso (Entra e Sai do alvo em loop)
local function PulseLoop(targetPlayer, actionCallback)
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not targetPlayer.Character then 
            connection:Disconnect() 
            return 
        end
        
        local THRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        local THum = targetPlayer.Character:FindFirstChild("Humanoid")
        
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Couch")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            
            local timer = tick() * 14 
            local dist = math.sin(timer) * 8 -- Movimento de entra e sai
            
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(0, 0.5, dist) * CFrame.Angles(0, timer, 0)
            vu7.Character.HumanoidRootPart.Velocity = Vector3.new(0,0,0)
            
            if THum.Sit then
                connection:Disconnect()
                actionCallback(PosOriginal)
            end
        end
    end)
end

local function FinalReset()
    _G.IsTrapping = false
    _G.FlingToggle = false
    pcall(function()
        vu7.Character.Humanoid:UnequipTools()
        vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
    end)
end

local function getPlayerNames()
    local pNames = {}
    for _, p in pairs(vu6:GetPlayers()) do if p ~= vu7 then table.insert(pNames, p.Name) end end
    return pNames
end

local function TeleportTo(cframe)
    if vu7.Character and vu7.Character:FindFirstChild("HumanoidRootPart") then
        vu7.Character:PivotTo(cframe)
    end
end
local vu6 = game:GetService("Players")
local vu7 = vu6.LocalPlayer
local vu8 = game:GetService("ReplicatedStorage")
local vu9 = game:GetService("Workspace")
local vu10 = game:GetService("RunService")
local vuCam = vu9.CurrentCamera

-- Variáveis de Controle
local walkSpeed, jumpPower, gravity = 16, 50, 192.2
local viewEnabled = false
_G.AntiSit = false
_G.AntiVoid = false
_G.RGBName = false
_G.RainbowBio = false

-- 3. FUNÇÕES DE SISTEMA
local function updateMovement()
    local char = vu7.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.WalkSpeed = walkSpeed
        char.Humanoid.JumpPower = jumpPower
        char.Humanoid.UseJumpPower = true
    end
    vu9.Gravity = gravity
end

local function updateCamera()
    if viewEnabled then
        local T = vu6:FindFirstChild(_G.SelectedPlayer)
        if T and T.Character and T.Character:FindFirstChildOfClass("Humanoid") then
            vuCam.CameraSubject = T.Character:FindFirstChildOfClass("Humanoid")
        end
    else
        vuCam.CameraSubject = vu7.Character:FindFirstChildOfClass("Humanoid")
    end
end

local function FinalReset()
    _G.IsTrapping = false
    pcall(function()
        local root = vu7.Character.HumanoidRootPart
        root.Velocity = Vector3.new(0,0,0)
        for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = true end end
    end)
end

-- 3. FUNÇÕES DE SISTEMA
local function TeleportTo(cframe)
    local char = vu7.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char:PivotTo(cframe)
    end
end

local function getPlayerNames()
    local pNames = {}
    for _, p in pairs(vu6:GetPlayers()) do if p ~= vu7 then table.insert(pNames, p.Name) end end
    return pNames
end
-- 3. JANELA PRINCIPAL
local Window = redzlib:MakeWindow({
    Title = "DARK x HUB",
    SubTitle = "Brookhaven RP | by Carlolx777",
    SaveFolder = "UnifiedHubConfig"
})

Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://131153193945220", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(0, 8) },
}) 
local InfoTab = Window:MakeTab({ Title = "Info", Icon = "rbxassetid://15309138473" })
InfoTab:AddSection({ "Informações do Script" })
InfoTab:AddParagraph({ "Owner / Developer:", "Carlolx777." })
InfoTab:AddParagraph({ "Colaborações:", "Carlolx777," })
InfoTab:AddParagraph({ "Você está usando:", "Dark x Hub Brookhaven RP" })
InfoTab:AddParagraph({"executor Delta:", })
InfoTab:AddParagraph({"Version1.0:", })
InfoTab:AddSection({ "Rejoin" })
InfoTab:AddButton({
    Name = "Rejoin",
    Callback = function()
        local TeleportService = game:GetService("TeleportService")
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
    end
})


-- --- ABA: TROLL ---
local TabTroll = Window:MakeTab({"Troll", "rbxassetid://13364900349"})
_G.SelectedPlayer = ""
TabTroll:AddSection({ "Selection" })
local PlayerDrop = TabTroll:AddDropdown({
    Name = "Lista de Player",
    Options = getPlayerNames(),
    Callback = function(v) _G.SelectedPlayer = v end
})
TabTroll:AddButton({"Atualizar Lista", function() PlayerDrop:Set(getPlayerNames()) end})

TabTroll:AddToggle({Name = "View Player (Ver Alvo)", Callback = function(s) viewEnabled = s updateCamera() end})

TabTroll:AddButton({Name = "Go To", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if T and T.Character then vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,0) end
end})
 

TabTroll:AddToggle({ Name = "Headsit", Callback = function() 
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if T then if not headsitActive then headsitOnPlayer(T) headsitActive = true else removeHeadsit() headsitActive = false end end 
end })

TabTroll:AddSection({ "Black Hole" })
-- Black Hole Fixo com Rotação XYZ
TabTroll:AddToggle({
    Name = "Black Hole",
    Default = false,
    Callback = function(state)
        _G.BlackHoleActive = state
        if state then
            _G.BH_Orbit_Active = false
            SetupBlackHole()
            BH_Connection = vu9.DescendantAdded:Connect(function(v)
                if _G.BlackHoleActive then ForcePart(v) end
            end)
            for _, v in ipairs(vu9:GetDescendants()) do ForcePart(v) end

            task.spawn(function()
                while _G.BlackHoleActive do
                    local T = vu6:FindFirstChild(_G.SelectedPlayer)
                    if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                        -- Rotação em todos os ângulos no próprio eixo do alvo
                        local t = tick() * 2
                        local rotation = CFrame.Angles(t, t/2, t/3) 
                        BH_Part.CFrame = T.Character.HumanoidRootPart.CFrame * rotation
                    end
                    vu10.Heartbeat:Wait()
                end
                if BH_Connection then BH_Connection:Disconnect() end
            end)
        end
    end
})

TabTroll:AddToggle({
    Name = "Black Hole Orbit",
    Default = false,
    Callback = function(state)
        _G.BH_Orbit_Active = state
        if state then
            _G.BlackHoleActive = false
            SetupBlackHole()
            BH_Connection = vu9.DescendantAdded:Connect(function(v)
                if _G.BH_Orbit_Active then ForcePart(v) end
            end)
            for _, v in ipairs(vu9:GetDescendants()) do ForcePart(v) end

            task.spawn(function()
                local angle = 0
                while _G.BH_Orbit_Active do
                    local T = vu6:FindFirstChild(_G.SelectedPlayer)
                    if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                        angle = angle + 10000000
                        local t = tick() * 1
                        -- Órbita combinada com rotação caótica XYZ
                        local radius = 7
                        local orbit = CFrame.Angles(0, math.rad(angle), 0) * CFrame.new(radius, math.sin(t)*2, 3)
                        local sphereRotation = CFrame.Angles(t, t/2, t/3)
                        
                        BH_Part.CFrame = T.Character.HumanoidRootPart.CFrame * orbit * sphereRotation
                    end
                    vu10.Heartbeat:Wait()
                end
                if BH_Connection then BH_Connection:Disconnect() end
            end)
        end
    end
})

TabTroll:AddSection({"Couch "})
TabTroll:AddButton({Name = "Kill", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000000000000000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1000000000
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*10.100) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(-21, -410, -42)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})
TabTroll:AddButton({Name = "Bring (Trazer)", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 100000000000000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 10000000000
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3,-2, math.sin(angle)*10.100) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.4)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "Couch")
                FinalReset()
            end
        end
    end)
end})

TabTroll:AddToggle({Name = "Fling (Fraco)", Callback = function(v)
    _G.FlingToggle = v
    if v then
        task.spawn(function()
            local PosOrig = vu7.Character.HumanoidRootPart.CFrame
            vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
            local angle = 10000000000000
            while _G.FlingToggle do
                vu10.Heartbeat:Wait()
                local T = vu6:FindFirstChild(_G.SelectedPlayer)
                if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                    local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
                    if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
                    angle = angle + 1000000000
                    vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(math.cos(angle)*1.3, -2, math.sin(angle)*10.100)
                    vu7.Character.HumanoidRootPart.Velocity = Vector3.new(900, 900, 900)
                    for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = false end end
                else break end
            end
            vu7.Character.Humanoid:UnequipTools()
            vu7.Character.HumanoidRootPart.CFrame = PosOrig
            FinalReset()
        end)
    else FinalReset() end
end})

TabTroll:AddToggle({Name = "Fling (Forte)", Callback = function(v)
    _G.FlingToggle = v
    if v then
        task.spawn(function()
            local PosOrig = vu7.Character.HumanoidRootPart.CFrame
            vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
            local angle = 10000000000000000000000000
            while _G.FlingToggle do
                vu10.Heartbeat:Wait()
                local T = vu6:FindFirstChild(_G.SelectedPlayer)
                if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                    local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
                    if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
                    angle = angle + 1000000000000
                    vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(math.cos(angle)*1.3, -4, math.sin(angle)*20.100)
                    vu7.Character.HumanoidRootPart.Velocity = Vector3.new(9000000, 9000000, 9000000)
                    for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = false end end
                else break end
            end
            vu7.Character.Humanoid:UnequipTools()
            vu7.Character.HumanoidRootPart.CFrame = PosOrig
            FinalReset()
        end)
    else FinalReset() end
end})
TabTroll:AddSection({"Teleportar o alvo para..."}) 
TabTroll:AddButton({Name = "Spawn", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000000000000000000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1000000000000
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*10.100) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(-26, 52, 16)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})

TabTroll:AddSection({"ADMIN"})
TabTroll:AddButton({Name = "Jail player V1", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000000000000000000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 100000000000
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*10.100) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(797, 5, 545)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)

local CasasTab = Window:MakeTab({
    Title = "Casas",
    Icon = "home"
})

-- ==================== MÚSICA NA CASA ====================
CasasTab:AddSection({("Música na Casa (Game Pass)")})

local selectedMusic = ""
local MusicsList = {
    { Name = "Leaked by WxDeveloper", Id = 10561310081763 },
    { Name = "Leaked by WxDeveloper", Id = 112651304685397 },
    { Name = "Leaked by WxDeveloper", Id = 16190782181 },
    { Name = "Leaked by WxDeveloper", Id = 1843497734 },
}

local Musics = {}
local MusicIds = {}

for _, music in ipairs(MusicsList) do
    table.insert(Musics, music.Name)
    MusicIds[music.Name] = music.Id
end

CasasTab:AddDropdown({
    Name = ("Selecionar Música"),
    Description = ("Funciona apenas com Game Pass"),
    Options = Musics,
    Default = "Músicas",
    Callback = function(Value)
        selectedMusic = Value
    end
})

CasasTab:AddTextBox({
    Name = ("Insira o ID da Música"),
    Description = ("Você pode colar o ID direto aqui"),
    PlaceholderText = "Ex: 74366765967475",
    Callback = function(value)
        local parsed = tonumber(value)
        if parsed then
            selectedMusic = parsed
        end
    end
})

CasasTab:AddButton({
    Name = ("Tocar Música"),
    Callback = function()
        local musicId = tonumber(selectedMusic) or MusicIds[selectedMusic]
        if musicId then
            local args = {
                [1] = "PickHouseMusicText",
                [2] = tostring(musicId)
            }
            local remote = game:GetService("ReplicatedStorage").RE:FindFirstChild("1Player1sHous1e")
            if remote then
                remote:FireServer(unpack(args))
            end
        end
    end
})

CasasTab:AddButton({
    Name = ("Parar Música"),
    Callback = function()
        local args = {
            [1] = "PickingHouseMusicStop",
            [2] = ""
        }
        local remote = game:GetService("ReplicatedStorage").RE:FindFirstChild("1Player1sHous1e")
        if remote then
            remote:FireServer(unpack(args))
        end
    end
})

-- ==================== FUNÇÕES AVANÇADAS DAS CASAS ====================
CasasTab:AddSection({("Funções Avançadas <Casas>")})

local selectedHouseNumber = nil

local function updateHouseList()
    local novaTabela = {}
    for _, v in pairs(workspace["001_Lots"]:GetChildren()) do
        table.insert(novaTabela, v.Name)
    end
    return novaTabela
end

local CasasOpem = CasasTab:AddDropdown({
    Name = ("Selecionar Uma Casa"),
    Description = "Select the <font color='rgb(88, 101, 242)'>House</font>",
    Options = updateHouseList(),
    Default = "Casas",
    Callback = function(Value)
        selectedHouseNumber = Value
    end
})

CasasTab:AddButton({
    Name = ("Atualizar Lista"),
    Callback = function()
        CasasOpem:Set(updateHouseList())
    end
})

CasasTab:AddButton({
    Name = ("Teleporta Para <Casa>"),
    Callback = function()
        if not selectedHouseNumber then return end
        local House = workspace["001_Lots"]:FindFirstChild(tostring(selectedHouseNumber))
        if House and game.Players.LocalPlayer.Character then
            local root = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if root then
                root.CFrame = CFrame.new(House.WorldPivot.Position)
            end
        end
    end
})

CasasTab:AddButton({
    Name = ("Teleporta Para O Cofre"),
    Callback = function()
        if not selectedHouseNumber then return end
        local house = workspace["001_Lots"]:FindFirstChild(selectedHouseNumber)
        if house and house:FindFirstChild("HousePickedByPlayer") then
            local safe = house.HousePickedByPlayer.HouseModel:FindFirstChild("001_Safe")
            if safe then
                local safeDoor = safe:FindFirstChild("SafeDoor")
                if safeDoor then
                    local root = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if root then
                        root.CFrame = safeDoor.CFrame
                    end
                end
            end
        end
    end
})

-- ==================== AUTO CLIQUE CAMPANHA E PORTA ====================
local autoClickEnabled = false
local autoKnockEnabled = false

local function autoClickDoorBell()
    while autoClickEnabled do
        if selectedHouseNumber then
            local house = workspace["001_Lots"]:FindFirstChild(selectedHouseNumber)
            if house and house:FindFirstChild("HousePickedByPlayer") then
                local doorBell = house.HousePickedByPlayer.HouseModel:FindFirstChild("001_DoorBell")
                if doorBell and doorBell:FindFirstChild("TouchBell") then
                    local clickDetector = doorBell.TouchBell:FindFirstChild("ClickDetector")
                    if clickDetector then
                        fireclickdetector(clickDetector)
                    end
                end
            end
        end
        task.wait(0.2)
    end
end

local function autoKnockDoor()
    while autoKnockEnabled do
        if selectedHouseNumber then
            local house = workspace["001_Lots"]:FindFirstChild(selectedHouseNumber)
            if house and house:FindFirstChild("HousePickedByPlayer") then
                local doors = house.HousePickedByPlayer.HouseModel:FindFirstChild("001_HouseDoors")
                if doors then
                    local frontDoor = doors:FindFirstChild("HouseDoorFront")
                    if frontDoor and frontDoor:FindFirstChild("Knock") and frontDoor.Knock:FindFirstChild("TouchBell") then
                        local clickDetector = frontDoor.Knock.TouchBell:FindFirstChild("ClickDetector")
                        if clickDetector then
                            fireclickdetector(clickDetector)
                        end
                    end
                end
            end
        end
        task.wait(0.2)
    end
end

CasasTab:AddToggle({
    Name = ("Auto Clique <Campainha>"),
    Description = ("Clica Na Campainha da casa"),
    Default = false,
    Callback = function(Value)
        autoClickEnabled = Value
        if autoClickEnabled then
            task.spawn(autoClickDoorBell)
        end
    end
})

CasasTab:AddToggle({
    Name = ("Auto Bate <Portas>"),
    Description = "Auto Bate Nas Portas Constantemente",
    Default = false,
    Callback = function(Value)
        autoKnockEnabled = Value
        if autoKnockEnabled then
            task.spawn(autoKnockDoor)
        end
    end
})

-- ==================== ATRAVESSAR PORTA ====================
CasasTab:AddToggle({
    Name = ("Atravessar Porta <Casa>"),
    Description = ("Auto Atravessar a porta da casa entre sem perm"),
    Default = false,
    Callback = function(Value)
        if not selectedHouseNumber then return end
        local House = workspace["001_Lots"]:FindFirstChild(tostring(selectedHouseNumber))
        if House and House:FindFirstChild("HousePickedByPlayer") then
            local doors = House.HousePickedByPlayer.HouseModel:FindFirstChild("001_HouseDoors")
            if doors and doors:FindFirstChild("HouseDoorFront") then
                for _, part in ipairs(doors.HouseDoorFront:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = not Value
                    end
                end
            end
        end
    end
})

-- ==================== CASA ASSOMBRADA ====================
CasasTab:AddSection({("Casa Assombrada")})


CasasTab:AddToggle({
    Name = ("Roleta Cor Da Casa"),
    Default = false,
    Callback = function(state)
        task.spawn(function()
            while state do
                if RemoteEvent then
                    RemoteEvent:FireServer("ColorPickHouse", getRandomColor())
                end
                task.wait(2)
            end
        end)
    end
})

CasasTab:AddToggle({
    Name = ("Roleta Cortinas"),
    Default = false,
    Callback = function(state)
        task.spawn(function()
            while state do
                if RemoteEvent then
                    RemoteEvent:FireServer("Curtains")
                end
                task.wait(1.5)
            end
        end)
    end
})

CasasTab:AddToggle({
    Name = ("Roleta Porta da Garagem"),
    Default = false,
    Callback = function(state)
        task.spawn(function()
            while state do
                if RemoteEvent then
                    RemoteEvent:FireServer("GarageDoor")
                end
                task.wait(5)
            end
        end)
    end
})

-- ==================== BANIR JOGADORES ====================
CasasTab:AddSection({("Banir Jogadores")})

local selectedPlayer = nil
local blacklistActive = false

local Players11 = CasasTab:AddDropdown({
    Name = ("Jogadores"),
    Options = {},
    Default = "Players",
    Callback = function(playerName)
        selectedPlayer = Players:FindFirstChild(playerName)
    end
})

CasasTab:AddButton({
    Name = ("Atualizar Lista"),
    Callback = function()
        local novaTabela = {}
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= Players.LocalPlayer then
                table.insert(novaTabela, v.Name)
            end
        end
        Players11:Set(novaTabela)
    end
})

CasasTab:AddToggle({
    Name = ("Auto Banimento"),
    Description = ("Banir automaticamente o jogador de todas as casas"),
    Default = false,
    Callback = function(state)
        blacklistActive = state
        if state and selectedPlayer then
            task.spawn(function()
                while blacklistActive and selectedPlayer do
                    local lots = workspace:FindFirstChild("001_Lots")
                    if lots then
                        for _, casa in pairs(lots:GetChildren()) do
                            local perm = casa:FindFirstChild("HousePickedByPlayer") 
                                and casa.HousePickedByPlayer.HouseModel 
                                and casa.HousePickedByPlayer.HouseModel:FindFirstChild("Permissions:Disallow")
                            if perm then
                                pcall(function()
                                    perm:FireServer(selectedPlayer)
                                end)
                            end
                        end
                    end
                    task.wait(1)
                end
            end)
        end
    end
})

-- ==================== FUNÇÕES AVANÇADAS DA CASA ====================
CasasTab:AddSection({("Funções Avançada Da Casa")})

local isHouseRGBActive = false
local repeatNames = false
local House1, House2, House3 = "", "", ""

local colors = {
    Color3.new(1, 0, 0), Color3.new(0, 1, 0), Color3.new(0, 0, 1),
    Color3.new(1, 1, 0), Color3.new(0, 1, 1), Color3.new(1, 0, 1)
}

local function changeHouseRGB()
    local remote = ReplicatedStorage.RE:FindFirstChild("1Player1sHous1e")
    while isHouseRGBActive do
        for _, color in ipairs(colors) do
            if not isHouseRGBActive then return end
            pcall(function()
                remote:FireServer("ColorPickHouse", color)
            end)
            task.wait(0.8)
        end
    end
end

CasasTab:AddTextBox({
    Title = ("Definir O Nome"),
    Description = ("Primeiro Nome Da Repetição!"),
    PlaceholderText = "Nick Casa",
    Callback = function(N) House1 = N end
})

CasasTab:AddTextBox({
    Title = ("Definir O Nome"),
    Description = ("Segundo Nome Da Repetição!"),
    PlaceholderText = "Nick Casa",
    Callback = function(N) House2 = N end
})

CasasTab:AddTextBox({
    Title = ("Definir O Nome"),
    Description = ("Terceiro Nome Da Repetição!"),
    PlaceholderText = "Nick Casa",
    Callback = function(N) House3 = N end
})

CasasTab:AddToggle({
    Name = ("Casa Lgbt"),
    Default = false,
    Callback = function(drip)
        isHouseRGBActive = drip
        if drip then
            task.spawn(changeHouseRGB)
        end
    end
})

CasasTab:AddToggle({
    Name = ("Repetir Nome da Casa"),
    Description = ("Repete os nomes automaticamente na casa"),
    Default = false,
    Callback = function(R)
        repeatNames = R
        if R then
            task.spawn(function()
                while repeatNames do
                    local remote = ReplicatedStorage.RE:FindFirstChild("1RPHous1eEven1t")
                    if remote then
                        remote:FireServer("BusinessName", House1)
                        task.wait(1)
                        remote:FireServer("BusinessName", House2)
                        task.wait(1)
                        remote:FireServer("BusinessName", House3)
                        task.wait(1)
                    end
                end
            end)
        end
    end
})

    

-- --- ABA: CARRO ---
local TabCar = Window:MakeTab({"Carro", "car"}) 

local Section = TabCar:AddSection({"Veículos"})
TabCar:AddTextBox({Name = "Velocidade do Carro", Callback = function(t)
    local car = vu9.Vehicles:FindFirstChild(vu7.Name .. "Car")
    if car then for _, s in pairs(car:GetDescendants()) do if s:IsA("VehicleSeat") and s:FindFirstChild("TopSpeed") then s.TopSpeed.Value = tonumber(t) or 100 end end end
end})
TabCar:AddButton({Name = "Bring All Cars (Trazer Carros)", Callback = function()
    local char = vu7.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root then
        for _, v in next, vu9.Vehicles:GetChildren() do
            pcall(function()
                v:SetPrimaryPartCFrame(root.CFrame)
            end)
        end
    end
end})

TabCar:AddButton({Name = "🚀 Fly Car (Voo de Carro)", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fly-car-44742"))() end})

-- --- ABA: TELEPORTES ---
local TabTP = Window:MakeTab({" Teleportes", "map"})

local teleportButtons = {
    {Nome = "Agência", Ponto = CFrame.new(179, 4, -464)},
    {Nome = "Oficina Oculta", Ponto = CFrame.new(0, 4, -495)},
    {Nome = "Bastidores", Ponto = CFrame.new(192, 4, 272)},
    {Nome = "Centro Urbano", Ponto = CFrame.new(136, 4, 117)},
    {Nome = " Área do Crime", Ponto = CFrame.new(-119, -28, 235)},
    {Nome = "Casa Deserta", Ponto = CFrame.new(986, 4, 63)},
    {Nome = "Arco", Ponto = CFrame.new(-589, 141, -59)},
    {Nome = " Hospital", Ponto = CFrame.new(-309, 4, 71)},
    {Nome = " Sala Secreta", Ponto = CFrame.new(-343, 4, -613)},
    {Nome = "Ilha Isolada", Ponto = CFrame.new(-1925, 23, 127)},
    {Nome = " Hotéis", Ponto = CFrame.new(182, 4, 150)},
    {Nome = "Montanha", Ponto = CFrame.new(-670, 251, 765)},
    {Nome = "Portal da Agência", Ponto = CFrame.new(672, 4, -296)},
    {Nome = "Esconderijo", Ponto = CFrame.new(505, -75, 143)},
    {Nome = "Escola", Ponto = CFrame.new(-312, 4, 211)},
    {Nome = "Café", Ponto = CFrame.new(161, 8, 52)},
    {Nome = "Cafeteria", Ponto = CFrame.new(-97.12, 4.65, 254.99)},
    {Nome = "Início", Ponto = CFrame.new(-26, 4, -23)},
    {Nome = "Banco", Ponto = CFrame.new(2.28, 4.65, 254.58)},
    {Nome = "Roupas", Ponto = CFrame.new(-46.15, 4.65, 253.20)},
    {Nome = "Refúgio", Ponto = CFrame.new(-88.48, 22.05, 262.34)},
    {Nome = "Clínica Dentária", Ponto = CFrame.new(-53.58, 22.15, 265.61)},
}

local selectedTeleport = nil
local opcoesTeleporte = {}
for _, entry in ipairs(teleportButtons) do table.insert(opcoesTeleporte, entry.Nome) end

TabTP:AddSection({" Locais Disponíveis"})

TabTP:AddDropdown({
    Name = "Escolha o Destino",
    Description = "Selecione um local para viajar",
    Options = opcoesTeleporte,
    Default = "Selecionar...",
    Callback = function(nomeSelecionado)
        for _, entry in ipairs(teleportButtons) do
            if entry.Nome == nomeSelecionado then
                selectedTeleport = entry
                break
            end
        end
    end
})

TabTP:AddButton({
    Name = "Teleportar",
    Callback = function()
        if selectedTeleport and vu7.Character then
            vu7.Character.HumanoidRootPart.CFrame = selectedTeleport.Ponto
        end
    end
})

TabTP:AddSection({"Gerenciamento de Servidor"})

TabTP:AddButton({
    Name = "Server Hop",
    Callback = function()
        local PlaceID = game.PlaceId
        local success, response = pcall(function()
            return game:HttpGet('https://games.roblox.com/v1/games/' .. PlaceID .. '/servers/Public?sortOrder=Asc&limit=100')
        end)
        
        if success then
            local Site = game.HttpService:JSONDecode(response)
            for _, v in pairs(Site.data) do
                if v.playing < v.maxPlayers and v.id ~= game.JobId then
                    StarterGui:SetCore("SendNotification", {
                        Title = "Server Hop",
                        Text = "Servidor encontrado com "..v.playing.." players!",
                        Duration = 5,
                        Icon = "rbxassetid://131153193945220"
                    })
                    task.wait(0.5)
                    game:GetService("TeleportService"):TeleportToPlaceInstance(PlaceID, v.id, vu7)
                    break
                end
            end
        end
    end
})

TabTP:AddButton({
    Name = "Rejoin",
    Callback = function()
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, vu7)
    end
})

-- --- LOOPS DE SISTEMA ---
local hue = 0
vu10.Heartbeat:Connect(function()
    hue = hue + 0.01 if hue > 1 then hue = 0 end
    local color = Color3.fromHSV(hue, 1, 1)
    
    if _G.RGBName then vu8.RE["1RPNam1eColo1r"]:FireServer("PickingRPNameColor", color) end
    if _G.RainbowBio then vu8.RE["1RPNam1eColo1r"]:FireServer("PickingRPBioColor", color) end
    
    local char = vu7.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if _G.AntiSit and hum then hum.Sit = false end
        
        local root = char:FindFirstChild("HumanoidRootPart")
        if _G.AntiVoid and root then
            local floor = vu9:FindFirstChild("AntiVoidFloor") or Instance.new("Part", vu9)
            floor.Name = "AntiVoidFloor"
            floor.Size = Vector3.new(2048, 2, 2048)
            floor.Anchored = true
            floor.Transparency = 1
            floor.Position = Vector3.new(root.Position.X, -490, root.Position.Z)
        elseif not _G.AntiVoid and vu9:FindFirstChild("AntiVoidFloor") then
            vu9.AntiVoidFloor:Destroy()
        end
    end
    if viewEnabled then updateCamera() end
end) 
local TrollTab = Window:MakeTab({"Controls", "settings"})
Window:SelectTab(TabTroll)
local Section = TrollTab:AddSection({"Funções"})
TrollTab:AddSection({ "Puxar Parts" })
TrollTab:AddButton({
    Name = "Puxar Parts",
    Description = " Ativando isso você puxa Parts até o seu personagem",
    Callback = function()
        local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")

local angle = 1
local radius = 10
local blackHoleActive = false

local function setupPlayer()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    local Folder = Instance.new("Folder", Workspace)
    local Part = Instance.new("Part", Folder)
    local Attachment1 = Instance.new("Attachment", Part)
    Part.Anchored = true
    Part.CanCollide = false
    Part.Transparency = 1

    return humanoidRootPart, Attachment1
end

local humanoidRootPart, Attachment1 = setupPlayer()

if not getgenv().Network then
    getgenv().Network = {
        BaseParts = {},
        Velocity = Vector3.new(100, 100, 100)
    }

    Network.RetainPart = function(part)
        if typeof(part) == "Instance" and part:IsA("BasePart") and part:IsDescendantOf(Workspace) then
            table.insert(Network.BaseParts, part)
            part.CustomPhysicalProperties = PhysicalProperties.new(80, 0, 0, 0, 0)
            part.CanCollide = false
        end
    end

    local function EnablePartControl()
        LocalPlayer.ReplicationFocus = Workspace
        RunService.Heartbeat:Connect(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            for _, part in pairs(Network.BaseParts) do
                if part:IsDescendantOf(Workspace) then
                    part.Velocity = Network.Velocity
                end
            end
        end)
    end

    EnablePartControl()
end

local function ForcePart(v)
    if v:IsA("Part") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
        for _, x in next, v:GetChildren() do
            if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then
                x:Destroy()
            end
        end
        if v:FindFirstChild("Attachment") then
            v:FindFirstChild("Attachment"):Destroy()
        end
        if v:FindFirstChild("AlignPosition") then
            v:FindFirstChild("AlignPosition"):Destroy()
        end
        if v:FindFirstChild("Torque") then
            v:FindFirstChild("Torque"):Destroy()
        end
        v.CanCollide = false
        
        local Torque = Instance.new("Torque", v)
        Torque.Torque = Vector3.new(100000000, 100000000, 100000000)
        local AlignPosition = Instance.new("AlignPosition", v)
        local Attachment2 = Instance.new("Attachment", v)
        Torque.Attachment0 = Attachment2
        AlignPosition.MaxForce = math.huge
        AlignPosition.MaxVelocity = math.huge
        AlignPosition.Responsiveness = 500
        AlignPosition.Attachment0 = Attachment2
        AlignPosition.Attachment1 = Attachment1
    end
end

local function toggleBlackHole()
    blackHoleActive = not blackHoleActive
    if blackHoleActive then
        for _, v in next, Workspace:GetDescendants() do
            ForcePart(v)
        end

        Workspace.DescendantAdded:Connect(function(v)
            if blackHoleActive then
                ForcePart(v)
            end
        end)

        spawn(function()
            while blackHoleActive and RunService.RenderStepped:Wait() do
                angle = angle + math.rad(2)

                local offsetX = math.cos(angle) * radius
                local offsetZ = math.sin(angle) * radius

                Attachment1.WorldCFrame = humanoidRootPart.CFrame * CFrame.new(offsetX, 0, offsetZ)
            end
        end)
    else
        Attachment1.WorldCFrame = CFrame.new(0, -1000, 0)
    end
end

LocalPlayer.CharacterAdded:Connect(function()
    humanoidRootPart, Attachment1 = setupPlayer()
    if blackHoleActive then
        toggleBlackHole()
    end
end)

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/miroeramaa/TurtleLib/main/TurtleUiLib.lua"))()
local window = library:Window("Projeto Darkzin")

window:Slider("Radius Blackhole",1,100,10, function(Value)
   radius = Value
end)

window:Toggle("Blackhole", true, function(Value)
       if Value then
            toggleBlackHole()
        else
            blackHoleActive = false
        end
end)

spawn(function()
    while true do
        RunService.RenderStepped:Wait()
        if blackHoleActive then
            angle = angle + math.rad(angleSpeed)
        end
    end
end)

toggleBlackHole()
    end
})

TrollTab:AddSection({ "Black Hole🕳" })
TrollTab:AddButton({
    Name = "Black Hole",
    Description = "Para usar, escreva o nome do alvo que deseja . aperte no botão de bring",
    Callback = function()
        -- Gui to Lua
-- Version: 3.2

-- Instances:

local Gui = Instance.new("ScreenGui")
local Main = Instance.new("Frame")
local Box = Instance.new("TextBox")
local UITextSizeConstraint = Instance.new("UITextSizeConstraint")
local Label = Instance.new("TextLabel")
local UITextSizeConstraint_2 = Instance.new("UITextSizeConstraint")
local Button = Instance.new("TextButton")
local UITextSizeConstraint_3 = Instance.new("UITextSizeConstraint")

--Properties:

Gui.Name = "Gui"
Gui.Parent = gethui()
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Main.Name = "Main"
Main.Parent = Gui
Main.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
Main.BorderColor3 = Color3.fromRGB(0, 0, 0)
Main.BorderSizePixel = 0
Main.Position = UDim2.new(0.335954279, 0, 0.542361975, 0)
Main.Size = UDim2.new(0.240350261, 0, 0.166880623, 0)
Main.Active = true
Main.Draggable = true

Box.Name = "Box"
Box.Parent = Main
Box.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Box.BorderColor3 = Color3.fromRGB(0, 0, 0)
Box.BorderSizePixel = 0
Box.Position = UDim2.new(0.0980926454, 0, 0.218712583, 0)
Box.Size = UDim2.new(0.801089942, 0, 0.364963502, 0)
Box.FontFace = Font.new("rbxasset://fonts/families/SourceSansSemibold.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Box.PlaceholderText = "Name player"
Box.Text = "DARK X HUB "
Box.TextColor3 = Color3.fromRGB(255, 255, 255)
Box.TextScaled = true
Box.TextSize = 31.000
Box.TextWrapped = true

UITextSizeConstraint.Parent = Box
UITextSizeConstraint.MaxTextSize = 31

Label.Name = "Label"
Label.Parent = Main
Label.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Label.BorderColor3 = Color3.fromRGB(0, 0, 0)
Label.BorderSizePixel = 0
Label.Size = UDim2.new(1, 0, 0.160583943, 0)
Label.FontFace = Font.new("rbxasset://fonts/families/Nunito.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Label.Text = "Black Hole | Deve By: Carlolx777"
Label.TextColor3 = Color3.fromRGB(255, 255, 255)
Label.TextScaled = true
Label.TextSize = 14.000
Label.TextWrapped = true

UITextSizeConstraint_2.Parent = Label
UITextSizeConstraint_2.MaxTextSize = 21

Button.Name = "Button"
Button.Parent = Main
Button.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Button.BorderColor3 = Color3.fromRGB(0, 0, 0)
Button.BorderSizePixel = 0
Button.Position = UDim2.new(0.183284417, 0, 0.656760991, 0)
Button.Size = UDim2.new(0.629427791, 0, 0.277372271, 0)
Button.Font = Enum.Font.Nunito
Button.Text = "Bring | Off"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextScaled = true
Button.TextSize = 28.000
Button.TextWrapped = true

UITextSizeConstraint_3.Parent = Button
UITextSizeConstraint_3.MaxTextSize = 28

-- Scripts:

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local character
local humanoidRootPart

mainStatus = true
UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
	if input.KeyCode == Enum.KeyCode.RightControl and not gameProcessedEvent then
		mainStatus = not mainStatus
		Main.Visible = mainStatus
	end
end)

local Folder = Instance.new("Folder", Workspace)
local Part = Instance.new("Part", Folder)
local Attachment1 = Instance.new("Attachment", Part)
Part.Anchored = true
Part.CanCollide = false
Part.Transparency = 1

if not getgenv().Network then
	getgenv().Network = {
		BaseParts = {},
		Velocity = Vector3.new(10000, 10000, 10000)
	}

	Network.RetainPart = function(Part)
		if Part:IsA("BasePart") and Part:IsDescendantOf(Workspace) then
			table.insert(Network.BaseParts, Part)
			Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
			Part.CanCollide = false
		end
	end

	local function EnablePartControl()
		LocalPlayer.ReplicationFocus = Workspace
		RunService.Heartbeat:Connect(function()
			sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
			for _, Part in pairs(Network.BaseParts) do
				if Part:IsDescendantOf(Workspace) then
					Part.Velocity = Network.Velocity
				end
			end
		end)
	end

	EnablePartControl()
end

local function ForcePart(v)
	if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
		for _, x in ipairs(v:GetChildren()) do
			if x:IsA("BodyMover") or x:IsA("RocketPropulsion") then
				x:Destroy()
			end
		end
		if v:FindFirstChild("Attachment") then
			v:FindFirstChild("Attachment"):Destroy()
		end
		if v:FindFirstChild("AlignPosition") then
			v:FindFirstChild("AlignPosition"):Destroy()
		end
		if v:FindFirstChild("Torque") then
			v:FindFirstChild("Torque"):Destroy()
		end
		v.CanCollide = false
		local Torque = Instance.new("Torque", v)
		Torque.Torque = Vector3.new(1000000000, 1000000000, 1000000000)
		local AlignPosition = Instance.new("AlignPosition", v)
		local Attachment2 = Instance.new("Attachment", v)
		Torque.Attachment0 = Attachment2
		AlignPosition.MaxForce = math.huge
		AlignPosition.MaxVelocity = math.huge
		AlignPosition.Responsiveness = 2000
		AlignPosition.Attachment0 = Attachment2
		AlignPosition.Attachment1 = Attachment1
	end
end

local blackHoleActive = false
local DescendantAddedConnection

local function toggleBlackHole()
	blackHoleActive = not blackHoleActive
	if blackHoleActive then
		Button.Text = "Bring | On"
		for _, v in ipairs(Workspace:GetDescendants()) do
			ForcePart(v)
		end

		DescendantAddedConnection = Workspace.DescendantAdded:Connect(function(v)
			if blackHoleActive then
				ForcePart(v)
			end
		end)

		spawn(function()
			while blackHoleActive and RunService.RenderStepped:Wait() do
				Attachment1.WorldCFrame = humanoidRootPart.CFrame
			end
		end)
	else
		Button.Text = "Bring | Off"
		if DescendantAddedConnection then
			DescendantAddedConnection:Disconnect()
		end
	end
end

local function getPlayer(name)
	local lowerName = string.lower(name)
	for _, p in pairs(Players:GetPlayers()) do
		local lowerPlayer = string.lower(p.Name)
		if string.find(lowerPlayer, lowerName) then
			return p
		elseif string.find(string.lower(p.DisplayName), lowerName) then
			return p
		end
	end
end

local player = nil

local function VDOYZQL_fake_script() -- Box.Script 
	local script = Instance.new('Script', Box)

	script.Parent.FocusLost:Connect(function(enterPressed)
		if enterPressed then
			player = getPlayer(Box.Text)
			if player then
				Box.Text = player.Name
				print("Player found:", player.Name)
			else
				print("Player not found")
			end
		end
	end)
end
coroutine.wrap(VDOYZQL_fake_script)()
local function JUBNQKI_fake_script() -- Button.Script 
	local script = Instance.new('Script', Button)

	script.Parent.MouseButton1Click:Connect(function()
		if player then
			character = player.Character or player.CharacterAdded:Wait()
			humanoidRootPart = character:WaitForChild("HumanoidRootPart")
			toggleBlackHole()
		else
			print("Player is not selected")
		end
	end)
end
coroutine.wrap(JUBNQKI_fake_script)()
    end
})
 local Section = TrollTab:AddSection({"Fantasia"})
TrollTab:AddButton({Name = "Ativar Goner", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Dboas123432sx/personagem/refs/heads/main/goner"))() end})
TrollTab:AddButton({Name = "Ativar Flash", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Dboas123432sx/personagem/refs/heads/main/Flash%20original"))() end})

local Section = TrollTab:AddSection({"Click TP"})
TrollTab:AddButton({Name = "TP Tool com som", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Teleport-Tools-34066"))() end})

 TrollTab:AddButton({
    Name = "Pegar Teleport Tool",
    Callback = function()
        local mouse = game.Players.LocalPlayer:GetMouse()
        
        local tool = Instance.new("Tool")
        tool.RequiresHandle = false
        tool.Name = "Tp Tool"
        
        tool.Activated:Connect(function()
            local pos = mouse.Hit.Position + Vector3.new(0, 2.5, 0)
            local targetCFrame = CFrame.new(pos.X, pos.Y, pos.Z)
            
            local character = game.Players.LocalPlayer.Character
            if character and character:FindFirstChild("HumanoidRootPart") then
                character.HumanoidRootPart.CFrame = targetCFrame
            end
        end)
        
        tool.Parent = game.Players.LocalPlayer.Backpack
    end
})

TrollTab:AddSection({"UNIVERSAL COMMADS ADMIN🔨"})
TrollTab:AddButton({Name = "Nameless Admin", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Nameless-admin-Universal-73082"))() end}) 
TrollTab:AddButton({Name = "Infinite Yield", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Infinite-Yeild-93384"))() end})
TrollTab:AddButton({Name = "Telekines", Callback = function()   loadstring(game:HttpGet('https://raw.githubusercontent.com/SAZXHUB/Control-update/main/README.md', true))() end})
TrollTab:AddButton({Name = "ShiftLock", Callback = function() loadstring(game:HttpGet("https://pastebin.com/raw/N2tiHgyv"))() end})
TrollTab:AddButton({Name = "Pegar Sofá", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/realgengar/scripts/refs/heads/main/Brancos.lua", true))() end})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local billboardGuis = {}
local connections = {}
local espEnabled = false
local selectedColor = "RGB"
local Section = TrollTab:AddSection({"ESP"})
TrollTab:AddDropdown({
    Name = "Cor do ESP",
    Default = "RGB",
    Options = {"RGB", "Branco", "Preto", "Vermelho", "Verde", "Azul", "Amarelo", "Rosa", "Roxo"},
    Callback = function(value) selectedColor = value end
})

local function getESPColor()
    if selectedColor == "RGB" then return Color3.fromHSV((tick() % 5) / 5, 1, 1) end
    local cols = {Branco=Color3.new(1,1,1), Preto=Color3.new(0,0,0), Vermelho=Color3.new(1,0,0), Verde=Color3.new(0,1,0), Azul=Color3.new(0,0.6,1), Amarelo=Color3.new(1,1,0), Rosa=Color3.new(1,0.4,0.7), Roxo=Color3.new(0.5,0,1)}
    return cols[selectedColor] or Color3.new(1,1,1)
end

local function updateESP(player)
    if player == Players.LocalPlayer or not espEnabled then return end
    local char = player.Character
    if char and char:FindFirstChild("Head") then
        if billboardGuis[player] then billboardGuis[player]:Destroy() end
        local bill = Instance.new("BillboardGui", char.Head)
        bill.Size = UDim2.new(0,200,0,50)
        bill.AlwaysOnTop = true
        bill.StudsOffset = Vector3.new(0,3,0)
        local lab = Instance.new("TextLabel", bill)
        lab.Size = UDim2.new(1,0,1,0)
        lab.BackgroundTransparency = 1
        lab.Text = player.Name .. " | " .. player.AccountAge .. "d"
        lab.TextColor3 = getESPColor()
        lab.Font = Enum.Font.SourceSansBold
        lab.TextSize = 14
        billboardGuis[player] = bill
    end
end

TrollTab:AddToggle({
    Name = "ESP Ativado",
    Description = "Mostra nome e idade da conta dos jogadores.",
    Default = false,
    Callback = function(value)
        espEnabled = value
        if not value then
            for _, v in pairs(billboardGuis) do v:Destroy() end
            billboardGuis = {}
        end
    end
})

RunService.Heartbeat:Connect(function()
    if espEnabled then
        for _, p in pairs(Players:GetPlayers()) do updateESP(p) end
    end
end)
