local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Yuna-ux/Other-scripts/refs/heads/main/UI-Library.lua"))()
local vu2 = loadstring(game:HttpGet("https://raw.githubusercontent.com/Laelmano24/brookhaven-tool/refs/heads/main/src/main.luau"))()
-- 1. SERVIÇOS E VARIÁVEIS
local vu6 = game:GetService("Players")
local vu7 = vu6.LocalPlayer
local vu8 = game:GetService("ReplicatedStorage")
local vu9 = game:GetService("Workspace")
local vu10 = game:GetService("RunService")
local vuCam = vu9.CurrentCamera

-- Variáveis de Controle
local walkSpeed, jumpPower, gravity = 16, 50, 192.2
local viewEnabled = false
_G.AntiSit = false
_G.AntiVoid = false
_G.RGBName = false
_G.RainbowBio = false
_G.SelectedPlayer = ""

-- Variáveis do Black Hole
_G.BlackHoleActive = false
_G.BH_Strength = 200
local BH_Attachment = nil
local BH_Part = nil

-- Variáveis de Áudio
getgenv().AudioLibrary = {} 
local audioSelecionado = ""
local idDigitado = ""
local nomeDigitado = ""

-- Variáveis de Controle
_G.SelectedPlayer = ""
local OrbitEnabled = false
local WingsEnabled = false
local RingEnabled = false
local RainbowEnabled = false
local KillEnabled = false
local FlingEnabled = false
local CachedProps = {}
local TargetCFrame = CFrame.new(-21, -410, -42) -- O CFrame solicitado

-- 2. FUNÇÕES BASE
local function UpdateProps()
    table.clear(CachedProps)
    local Folder = workspace:FindFirstChild("WorkspaceCom") and workspace.WorkspaceCom:FindFirstChild("001_TrafficCones")
    if not Folder then return end
    for _, prop in ipairs(Folder:GetChildren()) do
        if prop.Name:find("Prop" .. vu7.Name) then
            local remote = prop:FindFirstChild("SetCurrentCFrame")
            if remote then table.insert(CachedProps, { Model = prop, Remote = remote }) end
        end
    end
end


-- 2. FUNÇÕES DE SUPORTE (BACKEND)

-- Função de Câmera (CORREÇÃO PARA O VIEW FUNCIONAR)
local function updateCamera()
    pcall(function()
        if viewEnabled then
            local target = vu6:FindFirstChild(_G.SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = target.Character.Humanoid
            end
        else
            if vu7.Character and vu7.Character:FindFirstChild("Humanoid") then
                vuCam.CameraSubject = vu7.Character.Humanoid
            end
        end
    end)
end
-- 2. FUNÇÕES BASE
local function UpdateProps()
    table.clear(CachedProps)
    local Folder = workspace:FindFirstChild("WorkspaceCom") and workspace.WorkspaceCom:FindFirstChild("001_TrafficCones")
    if not Folder then return end
    for _, prop in ipairs(Folder:GetChildren()) do
        if prop.Name:find("Prop" .. vu7.Name) then
            local remote = prop:FindFirstChild("SetCurrentCFrame")
            if remote then table.insert(CachedProps, { Model = prop, Remote = remote }) end
        end
    end
end
local function DisableAllProps()
    OrbitEnabled = false
    WingsEnabled = false
    RingEnabled = false
    KillEnabled = false
    FlingEnabled = false
end
-- Configuração do Black Hole
local function SetupBlackHole()
    if not BH_Part then
        BH_Part = Instance.new("Part")
        BH_Part.Name = "BH_TargetPart"
        BH_Part.Anchored = true
        BH_Part.CanCollide = false
        BH_Part.Transparency = 1
        BH_Part.Parent = vu9
        BH_Attachment = Instance.new("Attachment", BH_Part)
    end
end

-- Network Bypass (Permite mover peças do mapa)
if not getgenv().NetworkBypassed then
    getgenv().NetworkBypassed = true
    task.spawn(function()
        while true do
            pcall(function()
                sethiddenproperty(vu7, "SimulationRadius", math.huge)
            end)
            vu10.Heartbeat:Wait()
        end
    end)
end

-- Forçar partes (Física do Black Hole)
local function ForcePart(v)
    if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and v.Name ~= "Handle" then
        for _, x in ipairs(v:GetChildren()) do
            if x:IsA("BodyMover") or x:IsA("AlignPosition") or x:IsA("Torque") then x:Destroy() end
        end
        v.CanCollide = false
        local torque = Instance.new("Torque", v)
        torque.Torque = Vector3.new(100000000, 1000000000, 100000000)
        local att0 = Instance.new("Attachment", v)
        torque.Attachment0 = att0
        local alignPos = Instance.new("AlignPosition", v)
        alignPos.Name = "BH_Force"
        alignPos.MaxForce = math.huge
        alignPos.MaxVelocity = math.huge
        alignPos.Responsiveness = _G.BH_Strength
        alignPos.Attachment0 = att0
        alignPos.Attachment1 = BH_Attachment
    end
end

-- Lógica de Pulso (Entra e Sai do alvo em loop)
local function PulseLoop(targetPlayer, actionCallback)
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not targetPlayer.Character then 
            connection:Disconnect() 
            return 
        end
        
        local THRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        local THum = targetPlayer.Character:FindFirstChild("Humanoid")
        
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Couch")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            
            local timer = tick() * 14 
            local dist = math.sin(timer) * 8 -- Movimento de entra e sai
            
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(0, 0.5, dist) * CFrame.Angles(0, timer, 0)
            vu7.Character.HumanoidRootPart.Velocity = Vector3.new(0,0,0)
            
            if THum.Sit then
                connection:Disconnect()
                actionCallback(PosOriginal)
            end
        end
    end)
end

local function FinalReset()
    _G.IsTrapping = false
    _G.FlingToggle = false
    pcall(function()
        vu7.Character.Humanoid:UnequipTools()
        vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
    end)
end

local function getPlayerNames()
    local pNames = {}
    for _, p in pairs(vu6:GetPlayers()) do if p ~= vu7 then table.insert(pNames, p.Name) end end
    return pNames
end

local function TeleportTo(cframe)
    if vu7.Character and vu7.Character:FindFirstChild("HumanoidRootPart") then
        vu7.Character:PivotTo(cframe)
    end
end
local vu6 = game:GetService("Players")
local vu7 = vu6.LocalPlayer
local vu8 = game:GetService("ReplicatedStorage")
local vu9 = game:GetService("Workspace")
local vu10 = game:GetService("RunService")
local vuCam = vu9.CurrentCamera

-- Variáveis de Controle
local walkSpeed, jumpPower, gravity = 16, 50, 192.2
local viewEnabled = false
_G.AntiSit = false
_G.AntiVoid = false
_G.RGBName = false
_G.RainbowBio = false

-- 3. FUNÇÕES DE SISTEMA
local function updateMovement()
    local char = vu7.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.WalkSpeed = walkSpeed
        char.Humanoid.JumpPower = jumpPower
        char.Humanoid.UseJumpPower = true
    end
    vu9.Gravity = gravity
end

local function updateCamera()
    if viewEnabled then
        local T = vu6:FindFirstChild(_G.SelectedPlayer)
        if T and T.Character and T.Character:FindFirstChildOfClass("Humanoid") then
            vuCam.CameraSubject = T.Character:FindFirstChildOfClass("Humanoid")
        end
    else
        vuCam.CameraSubject = vu7.Character:FindFirstChildOfClass("Humanoid")
    end
end

local function FinalReset()
    _G.IsTrapping = false
    pcall(function()
        local root = vu7.Character.HumanoidRootPart
        root.Velocity = Vector3.new(0,0,0)
        for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = true end end
    end)
end

-- 3. FUNÇÕES DE SISTEMA
local function TeleportTo(cframe)
    local char = vu7.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        char:PivotTo(cframe)
    end
end

local function getPlayerNames()
    local pNames = {}
    for _, p in pairs(vu6:GetPlayers()) do if p ~= vu7 then table.insert(pNames, p.Name) end end
    return pNames
end
-- 3. JANELA PRINCIPAL
local Window = redzlib:MakeWindow({
    Title = "DARK x HUB",
    SubTitle = "Brookhaven RP | by Carlolx777",
    SaveFolder = "UnifiedHubConfig"
})

Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://131153193945220", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(0, 8) },
}) 
local InfoTab = Window:MakeTab({ Title = "Info", Icon = "rbxassetid://15309138473" })
InfoTab:AddSection({ "Informações do Script" })
InfoTab:AddParagraph({ "Owner / Developer:", "Carlolx777." })
InfoTab:AddParagraph({ "Colaborações:", "Carlolx777," })
InfoTab:AddParagraph({ "Você está usando:", "Dark x Hub Brookhaven RP" })
InfoTab:AddParagraph({"executor Delta:", })
InfoTab:AddParagraph({"Version1.0:", })
InfoTab:AddSection({ "Rejoin" })
InfoTab:AddButton({
    Name = "Rejoin",
    Callback = function()
        local TeleportService = game:GetService("TeleportService")
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
    end
})


-- --- ABA: TROLL ---
local TabTroll = Window:MakeTab({"Troll", "rbxassetid://13364900349"})
_G.SelectedPlayer = ""
TabTroll:AddSection({ "Selection" })
local PlayerDrop = TabTroll:AddDropdown({
    Name = "Lista de Player",
    Options = getPlayerNames(),
    Callback = function(v) _G.SelectedPlayer = v end
})
TabTroll:AddButton({"Atualizar Lista", function() PlayerDrop:Set(getPlayerNames()) end})

TabTroll:AddToggle({Name = "View Player (Ver Alvo)", Callback = function(s) viewEnabled = s updateCamera() end})

TabTroll:AddButton({Name = "Go To", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if T and T.Character then vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,0) end
end})
 

TabTroll:AddSection({ "Black Hole🕳️ XYZ" })

-- Black Hole Fixo com Rotação XYZ
TabTroll:AddToggle({
    Name = "Black Hole Fixo (Rotação XYZ)",
    Default = false,
    Callback = function(state)
        _G.BlackHoleActive = state
        if state then
            _G.BH_Orbit_Active = false
            SetupBlackHole()
            BH_Connection = vu9.DescendantAdded:Connect(function(v)
                if _G.BlackHoleActive then ForcePart(v) end
            end)
            for _, v in ipairs(vu9:GetDescendants()) do ForcePart(v) end

            task.spawn(function()
                while _G.BlackHoleActive do
                    local T = vu6:FindFirstChild(_G.SelectedPlayer)
                    if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                        -- Rotação em todos os ângulos no próprio eixo do alvo
                        local t = tick() * 2
                        local rotation = CFrame.Angles(t, t/2, t/3) 
                        BH_Part.CFrame = T.Character.HumanoidRootPart.CFrame * rotation
                    end
                    vu10.Heartbeat:Wait()
                end
                if BH_Connection then BH_Connection:Disconnect() end
            end)
        end
    end
})

-- Black Hole Orbit com Rotação XYZ
TabTroll:AddToggle({
    Name = "Black Hole Orbit (Giro XYZ)",
    Default = false,
    Callback = function(state)
        _G.BH_Orbit_Active = state
        if state then
            _G.BlackHoleActive = false
            SetupBlackHole()
            BH_Connection = vu9.DescendantAdded:Connect(function(v)
                if _G.BH_Orbit_Active then ForcePart(v) end
            end)
            for _, v in ipairs(vu9:GetDescendants()) do ForcePart(v) end

            task.spawn(function()
                local angle = 0
                while _G.BH_Orbit_Active do
                    local T = vu6:FindFirstChild(_G.SelectedPlayer)
                    if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                        angle = angle + 10000000
                        local t = tick() * 1
                        -- Órbita combinada com rotação caótica XYZ
                        local radius = 7
                        local orbit = CFrame.Angles(0, math.rad(angle), 0) * CFrame.new(radius, math.sin(t)*2, 3)
                        local sphereRotation = CFrame.Angles(t, t/2, t/3)
                        
                        BH_Part.CFrame = T.Character.HumanoidRootPart.CFrame * orbit * sphereRotation
                    end
                    vu10.Heartbeat:Wait()
                end
                if BH_Connection then BH_Connection:Disconnect() end
            end)
        end
    end
})
TabTroll:AddSection({ "Ações de Props" })

TabTroll:AddButton({Name = "Bring Props (Trazer Cones)", Callback = function()
    local target = vu6:FindFirstChild(_G.SelectedPlayer)
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local targetPos = target.Character.HumanoidRootPart.CFrame
        for _, data in ipairs(CachedProps) do
            pcall(function() data.Remote:InvokeServer(targetPos) end)
        end
    end
end})

-- FUNÇÃO KILL (Usa o CFrame solicitado para puxar o player)
TabTroll:AddToggle({Name = "Kill Props (Matar)", Callback = function(s) 
    DisableAllProps()
    KillEnabled = s 
end})

-- FUNÇÃO FLING (Usa o CFrame com rotação para arremessar)
TabTroll:AddToggle({Name = "Fling Props (Arremessar)", Callback = function(s) 
    DisableAllProps()
    FlingEnabled = s 
end})

TabTroll:AddSection({ "Animações" })

TabTroll:AddToggle({Name = "Wings (Asas)", Callback = function(s) DisableAllProps() WingsEnabled = s end})
TabTroll:AddToggle({Name = "Orbit (Orbitar)", Callback = function(s) DisableAllProps() OrbitEnabled = s end})
TabTroll:AddToggle({Name = "Rainbow", Callback = function(s) RainbowEnabled = s end})

TabTroll:AddButton({Name = "Reset Props", Callback = function()
    DisableAllProps()
    for _, data in ipairs(CachedProps) do
        pcall(function() data.Remote:InvokeServer(CFrame.new(0, -999, 0)) end)
    end
end})

-- 4. LOOP DE FÍSICA (RENDERSTEPPED)
vu10.RenderStepped:Connect(function()
    local targetPlayer = vu6:FindFirstChild(_G.SelectedPlayer)
    if not targetPlayer or not targetPlayer.Character then return end
    local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not targetHRP then return end

    local count = #CachedProps
    if count <= 0 then return end
    
    local time = tick()

    for i, data in ipairs(CachedProps) do
        local finalCF

        if KillEnabled then
            -- Alterna entre o player e o CFrame alvo para bugar a física e matá-lo
            data.Remote:InvokeServer(targetHRP.CFrame)
            data.Remote:InvokeServer(TargetCFrame)
            continue -- Pula o invoke final para evitar lentidão

        elseif FlingEnabled then
            -- Gira violentamente e envia para o CFrame alvo
            local rotation = CFrame.Angles(math.random() * math.pi, math.random() * math.pi, math.random() * math.pi)
            data.Remote:InvokeServer(targetHRP.CFrame * rotation)
            data.Remote:InvokeServer(TargetCFrame)
            continue

        elseif OrbitEnabled then
            local radius = 15
            local angle = ((i - 1) / count) * math.pi * 2 + (time * 2)
            local offset = Vector3.new(math.cos(angle) * radius, -2, math.sin(angle) * radius)
            finalCF = targetHRP.CFrame * CFrame.new(offset)

        elseif WingsEnabled then
            local side = i - ((count + 1) / 2)
            local flap = math.sin(time * 5)
            local offset = Vector3.new(side * 4, (math.abs(side) * 2) + flap, 2)
            local rotation = CFrame.Angles(0, 0, -math.rad(side * 15))
            finalCF = targetHRP.CFrame * CFrame.new(offset) * rotation
        end

        if finalCF then
            pcall(function() data.Remote:InvokeServer(finalCF) end)
        end
    end
end)

-- LOOP RAINBOW
task.spawn(function()
    while true do
        if RainbowEnabled then
            local folder = workspace:FindFirstChild("WorkspaceCom") and workspace.WorkspaceCom:FindFirstChild("001_TrafficCones")
            if folder then
                for _, prop in ipairs(folder:GetChildren()) do
                    if prop.Name:find("Prop" .. vu7.Name) then
                        local remote = prop:FindFirstChild("ChangePropColor")
                        if remote then remote:InvokeServer(Color3.fromHSV((tick()*0.2)%1, 1, 1)) end
                    end
                end
            end
        end
        task.wait(0.1)
    end
end)

UpdateProps()
workspace.DescendantAdded:Connect(function() task.wait(0.5) UpdateProps() end)

TabTroll:AddSection({"Ações com Sofá "})
TabTroll:AddButton({Name = "Kill", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1.4 
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*10.0) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(-21, -410, -42)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})
TabTroll:AddButton({Name = "Bring (Trazer)", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1.4 
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.08, math.sin(angle)*10.10) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.4)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "Couch")
                FinalReset()
            end
        end
    end)
end})

TabTroll:AddToggle({Name = "Fling (Fraco)", Callback = function(v)
    _G.FlingToggle = v
    if v then
        task.spawn(function()
            local PosOrig = vu7.Character.HumanoidRootPart.CFrame
            vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
            local angle = 100
            while _G.FlingToggle do
                vu10.Heartbeat:Wait()
                local T = vu6:FindFirstChild(_G.SelectedPlayer)
                if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                    local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
                    if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
                    angle = angle + 1.4 
                    vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(math.cos(angle)*1.5,1.-3, math.sin(angle)*15.0)
                    vu7.Character.HumanoidRootPart.Velocity = Vector3.new(900, 900, 900)
                    for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = false end end
                else break end
            end
            vu7.Character.Humanoid:UnequipTools()
            vu7.Character.HumanoidRootPart.CFrame = PosOrig
            FinalReset()
        end)
    else FinalReset() end
end})

TabTroll:AddToggle({Name = "Fling (Forte)", Callback = function(v)
    _G.FlingToggle = v
    if v then
        task.spawn(function()
            local PosOrig = vu7.Character.HumanoidRootPart.CFrame
            vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
            local angle = 100
            while _G.FlingToggle do
                vu10.Heartbeat:Wait()
                local T = vu6:FindFirstChild(_G.SelectedPlayer)
                if T and T.Character and T.Character:FindFirstChild("HumanoidRootPart") then
                    local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
                    if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
                    angle = angle + 1.4 
                    vu7.Character.HumanoidRootPart.CFrame = T.Character.HumanoidRootPart.CFrame * CFrame.new(math.cos(angle)*1.3,1.-3, math.sin(angle)*50.90)
                    vu7.Character.HumanoidRootPart.Velocity = Vector3.new(900000, 900000, 900000)
                    for _, p in pairs(vu7.Character:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = false end end
                else break end
            end
            vu7.Character.Humanoid:UnequipTools()
            vu7.Character.HumanoidRootPart.CFrame = PosOrig
            FinalReset()
        end)
    else FinalReset() end
end})
TabTroll:AddSection({"Teleportar o alvo para..."}) 
TabTroll:AddButton({Name = "Spawn", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1.4 
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*10.0) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(-26, 52, 16)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})
TabTroll:AddButton({Name = "Casa da boneca", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 9
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.5, 1.0, math.sin(angle)*20.10) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(-80562686937298293739389484948498452544, 8056269283938398393729749388652544, -6364027420602838397397398393872)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})

TabTroll:AddSection({"ADMIN"})
TabTroll:AddButton({Name = "Jail player V1", Callback = function()
    local T = vu6:FindFirstChild(_G.SelectedPlayer)
    if not T or not T.Character then return end
    _G.IsTrapping = true
    local PosOriginal = vu7.Character.HumanoidRootPart.CFrame
    vu8.RE["1Too1l"]:InvokeServer("PickingTools", "Couch")
    local angle = 1000
    local connection
    connection = vu10.Heartbeat:Connect(function()
        if not _G.IsTrapping or not T.Character then connection:Disconnect() FinalReset() return end
        local THRP = T.Character:FindFirstChild("HumanoidRootPart")
        local THum = T.Character:FindFirstChild("Humanoid")
        if THRP and THum then
            local Tool = vu7.Backpack:FindFirstChild("Couch") or vu7.Character:FindFirstChild("Stretcher")
            if Tool then vu7.Character.Humanoid:EquipTool(Tool) end
            angle = angle + 1.4 
            vu7.Character.HumanoidRootPart.CFrame = THRP.CFrame * CFrame.new(math.cos(angle)*1.3, 1.8, math.sin(angle)*20.10) * CFrame.Angles(0, angle, angle/2)
            if THum.Sit then
                connection:Disconnect()
                vu7.Character.HumanoidRootPart.CFrame = CFrame.new(797, 5, 545)
                task.wait(0.3)
                vu7.Character.Humanoid:UnequipTools() -- Desequipa automaticamente
                task.wait(0.2)
                vu7.Character.HumanoidRootPart.CFrame = PosOriginal
                task.wait(0.1)
                vu8.RE["1Clea1rToo1ls"]:FireServer("PlayerWantsToDeleteTool", "ClearAllTools")
                task.wait(0.2)
        FinalReset()
            end
        end
    end)
end})
TabTroll:AddSection({ "Antis" })
TabTroll:AddToggle({ Name = "Anti-Void", Callback = function(s) _G.AntiVoid = s end })
TabTroll:AddToggle({ Name = "Anti-Sit", Callback = function(s) _G.AntiSit = s end })
-- 🎵 Aba Audio All
local AudioAllTab = Window:MakeTab({
    Title = "Audio All",
    Icon = "music"
})

-- Create a section
AudioAllTab:AddSection({"Audio Todos os Players"})

-- Lista de áudios
local audios = {
    {Name = "Yamete Kudasai", ID = 108494476595033},
    {Name = "Gritinho", ID = 5710016194},
    {Name = "Jumpscare Horroroso", ID = 85435253347146},
    {Name = "udio Alto", ID = 6855150757},
    {Name = "Rudo", ID = 120034877160791},
    {Name = "Jumpscare 2", ID = 110637995610528},
    {Name = "Risada Da Bruxa Minecraft", ID = 116214940486087},
    {Name = "The Boiled One", ID = 137177653817621}
}

local selectedAudioID

-- Adicionar uma textbox para inserir o ID do áudio
AudioAllTab:AddTextBox({
    Name = "Insira o ID do áudio ou Musica",
    Description = "Digite o ID do áudio",
    PlaceholderText = "ID do áudio",
    Callback = function(value)
        selectedAudioID = tonumber(value)
    end
})

-- Adicionar uma dropdown para selecionar o áudio
local audioNames = {}
for _, audio in ipairs(audios) do
    table.insert(audioNames, audio.Name)
end

AudioAllTab:AddDropdown({
    Name = "Selecione o áudio",
    Description = "Escolha um áudio da lista",
    Options = audioNames,
    Default = audioNames[1],
    Flag = "selected_audio",
    Callback = function(value)
        for _, audio in ipairs(audios) do
            if audio.Name == value then
                selectedAudioID = audio.ID
                break
            end
        end
    end
})

-- Controle do loop
local audioLoop = false

-- Nova seção para loop de áudio
AudioAllTab:AddSection({"Loop de Audio"})

-- Função para tocar o áudio repetidamente
local function playLoopedAudio()
    while audioLoop do
        if selectedAudioID then
            local args = {
                [1] = game:GetService("Workspace"),
                [2] = selectedAudioID,
                [3] = 1,
            }
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Gu1nSound1s"):FireServer(unpack(args))

            -- Criar e tocar o áudio
            local sound = Instance.new("Sound")
            sound.SoundId = "rbxassetid://" .. selectedAudioID
            sound.Parent = game.Players.LocalPlayer.Character.HumanoidRootPart
            sound:Play()
        else
            warn("Nenhum áudio selecionado!")
        end

        task.wait(0.5) -- Pequeno delay para evitar sobrecarga
    end
end

-- Toggle para loop de áudio
AudioAllTab:AddToggle({
    Name = "Loop Tocar áudio",
    Description = "Ativa o loop do áudio",
    Default = false,
    Flag = "audio_loop",
    Callback = function(value)
        audioLoop = value
        if audioLoop then
            task.spawn(playLoopedAudio) -- Inicia o loop em uma nova thread
        end
    end
})

-- Adicionar um parágrafo como label
AudioAllTab:AddParagraph({"Info", "Loop de tocar áudio (Todos players do Server ouvem)"})

-- Função para tocar o áudio normal
local function playAudio()
    if selectedAudioID then
        local args = {
            [1] = game:GetService("Workspace"),
            [2] = selectedAudioID,
            [3] = 1,
        }
        game:GetService("ReplicatedStorage").RE:FindFirstChild("1Gu1nSound1s"):FireServer(unpack(args))

        -- Criar e tocar o áudio
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://" .. selectedAudioID
        sound.Parent = game.Players.LocalPlayer.Character.HumanoidRootPart
        sound:Play()
    else
        warn("Nenhum áudio selecionado!")
    end
end

-- Nova seção para tocar áudio
AudioAllTab:AddSection({"Tocar áudio"})

-- Botão para tocar o áudio
AudioAllTab:AddButton({
    Name = "Tocar áudio",
    Callback = function()
        playAudio()
    end
})

-- Parte extra para Audio All (client + server side)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local audioID = 6314880174 -- ID fixo do áudio

local function Audio_All_ClientSide(ID)
    local function CheckFolderAudioAll()
        local FolderAudio = workspace:FindFirstChild("Audio all client")
        if not FolderAudio then
            FolderAudio = Instance.new("Folder")
            FolderAudio.Name = "Audio all client"
            FolderAudio.Parent = workspace
        end
        return FolderAudio
    end

    local function CreateSound(ID)
        if type(ID) ~= "number" then
            print("Insira um número válido!")
            return nil
        end

        local Folder_Audio = CheckFolderAudioAll()
        if Folder_Audio then
            local Sound = Instance.new("Sound")
            Sound.SoundId = "rbxassetid://" .. ID
            Sound.Volume = 1
            Sound.Looped = false
            Sound.Parent = Folder_Audio
            Sound:Play()
            task.wait(1)
            Sound:Destroy()
        end
    end

    CreateSound(ID)
end

local function Audio_All_ServerSide(ID)
    if type(ID) ~= "number" then
        print("Insira um número válido!")
        return nil
    end

    local GunSoundEvent = ReplicatedStorage:FindFirstChild("1Gu1nSound1s", true)
    if GunSoundEvent then
        GunSoundEvent:FireServer(workspace, ID, 1)
    end
end

-- Toggle para "Estorar ouvido de geral"
AudioAllTab:AddToggle({
    Name = "Estorar ouvido de geral KK",
    Description = "Toca áudio repetidamente para todos",
    Default = false,
    Flag = "audio_spam",
    Callback = function(value)
        getgenv().Audio_All_loop_fast = value

        while getgenv().Audio_All_loop_fast do
            Audio_All_ServerSide(audioID)
            task.spawn(function()
                Audio_All_ClientSide(audioID)
            end)
            task.wait(0.03) -- Delay extremamente rápido
        end
    end
})

AudioAllTab:AddParagraph({"Info", "Todos do server ouvem o áudio"})

local TabVisual = Window:MakeTab({"Visual", "Star"})
local Section = TabVisual:AddSection({"Visual Player"})
TabVisual:AddSection({"Deixar o Nome do Player Rainbow🌈 "})
TabVisual:AddToggle({
    Name = "Nome RGB",
    Description = "Nome do RP",
    Default = false,
    Callback = function(s)
        _G.RGBName = s
    end })   
    
TabVisual:AddSection({"Deixar a Bio do Player Rainbow🌈"})
TabVisual:AddToggle({
    Name = "Bio RGB",
    Description = "Bio Player",
    Default = false,
    Callback = function(s)
        _G.RainbowBio = s
    end })

local colors = { "Bright red", "Lime green", "Bright blue", "Bright yellow", "Bright cyan", "Hot pink", "Royal purple" }
local rgbEnabled = false

local function changeColor(color)
    local args = { color }
    game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeBodyColor"):FireServer(unpack(args))
end

local function toggleRGBCharacter(enabled)
    rgbEnabled = enabled
    if rgbEnabled then
        while rgbEnabled do
            for _, color in ipairs(colors) do
                if not rgbEnabled then return end
                changeColor(color)
                wait(0.5)
            end
        end
       end
    end   
    
TabVisual:AddSection({"Deixar o seu personagem Rainbow🌈"})
TabVisual:AddToggle({
    Name = "RGB Character",
    Description = "Deixa seu personagem RGB",
    Default = false,
    Callback = function(value)
        toggleRGBCharacter(value)
    end
})

TabVisual:AddSection({ "Cabelo RGB" })
local hairColors = {
    Color3.new(1, 1, 0), Color3.new(0, 0, 1), Color3.new(1, 0, 1), Color3.new(1, 1, 1),
    Color3.new(0, 1, 0), Color3.new(0.5, 0, 1), Color3.new(1, 0.647, 0), Color3.new(0, 1, 1)
}
local isActive = false


local function changeHairColor()
    local i = 1
    while isActive do
        if not isActive then break end
        local args = { [1] = "ChangeHairColor2", [2] = hairColors[i] }
        game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Max1y"):FireServer(unpack(args))
        wait(0.1)
        i = i % #hairColors + 1
    end
end 
 TabVisual:AddSection({"Deixar o seu cabelo Rainbow🌈"})  
TabVisual:AddToggle({
    Name = "Cabelo RGB",
    Description = "Deixa Seu Cabelo RGB",
    Default = false,
    Callback = function(value)
        isActive = value
        if isActive then
            changeHairColor()
        end
    end
})
TabVisual:AddSection({ "Invisível FE" })
TabVisual:AddButton({
    Name = "Ficar Invisível",
    Description = "Ficar invisível FE",
Callback = function()
        
        local args = {
    [1] = {
        [1] = 102344834840946,
        [2] = 70400527171038,
        [3] = 0,
        [4] = 0,
        [5] = 0,
        [6] = 0
    }
}

game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Wear"):InvokeServer(111858803548721)
local allaccessories = {}

for zxcwefwfecas, xcaefwefas in ipairs({
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.BackAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.FaceAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.FrontAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.NeckAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.HatAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.HairAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.ShouldersAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.WaistAccessory,
    game.Players.LocalPlayer.Character.Humanoid.HumanoidDescription.GraphicTShirt
}) do
    for scacvdfbdb in string.gmatch(xcaefwefas, "%d+") do
        table.insert(allaccessories, tonumber(scacvdfbdb))
    end
end

wait()

for asdwr,asderg in ipairs(allaccessories) do
    task.spawn(function()
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Wear"):InvokeServer(asderg)
        print(asderg)
    end)
end
    end
})
--  Aba Casas / Home
local CasasTab = Window:MakeTab({
    Title = "Casas",
    Icon = "home"
})

-- Variáveis
local isUnbanActive = false
local SelectHouse = nil
local NoclipDoor = nil

-- Função para obter lista de casas
local function getHouseList()
    local Tabela = {}
    local lots = workspace:FindFirstChild("001_Lots")
    if lots then
        for _, House in ipairs(lots:GetChildren()) do
            if House.Name ~= "For Sale" and House:IsA("Model") then
                table.insert(Tabela, House.Name)
            end
        end
    end
    return Tabela
end

-- Dropdown para selecionar casas
pcall(function()
    CasasTab:AddDropdown({
        Name = "Selecione a Casa",
        Options = getHouseList(),
        Default = "...",
        Callback = function(Value)
            SelectHouse = Value
            if NoclipDoor then
                NoclipDoor:Set(false)
            end
            print("Casa selecionada: " .. tostring(Value))
        end
    })
end)

-- Função para atualizar a lista de casas
local function DropdownHouseUpdate()
    local Tabela = getHouseList()
    pcall(function()
        CasasTab:ClearDropdown("Selecione a Casa") -- Limpar dropdown, se suportado
        CasasTab:AddDropdown({
            Name = "Selecione a Casa",
            Options = Tabela,
            Default = "...",
            Callback = function(Value)
                SelectHouse = Value
                if NoclipDoor then
                    NoclipDoor:Set(false)
                end
            end
        })
    end)
end

-- Botão para atualizar lista de casas
pcall(function()
    CasasTab:AddButton({
        Name = "Atualizar Lista de Casas",
        Callback = function()
            pcall(DropdownHouseUpdate)
        end
    })
end)

-- Teleportar para casa
pcall(function()
    CasasTab:AddButton({
        Name = "Teleportar para Casa",
        Callback = function()
            local House = workspace["001_Lots"]:FindFirstChild(tostring(SelectHouse))
            if House and game.Players.LocalPlayer.Character then
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(House.WorldPivot.Position)
            else
                print("Casa no encontrada: " .. tostring(SelectHouse))
            end
        end
    })
end)

-- Teleportar para cofre
pcall(function()
    CasasTab:AddButton({
        Name = "Teleportar para Cofre",
        Callback = function()
            local House = workspace["001_Lots"]:FindFirstChild(tostring(SelectHouse))
            if House and House:FindFirstChild("HousePickedByPlayer") and game.Players.LocalPlayer.Character then
                local safe = House.HousePickedByPlayer.HouseModel:FindFirstChild("001_Safe")
                if safe then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(safe.WorldPivot.Position)
                else
                    print("Cofre no encontrado na casa: " .. tostring(SelectHouse))
                end
            else
                print("Casa no encontrada: " .. tostring(SelectHouse))
            end
        end
    })
end)

-- Toggle para atravessar porta
pcall(function()
    NoclipDoor = CasasTab:AddToggle({
        Name = "Atravessar Porta da Casa",
        Description = "",
        Default = false,
        Callback = function(Value)
            pcall(function()
                local House = workspace["001_Lots"]:FindFirstChild(tostring(SelectHouse))
                if House and House:FindFirstChild("HousePickedByPlayer") then
                    local housepickedbyplayer = House.HousePickedByPlayer
                    local doors = housepickedbyplayer.HouseModel:FindFirstChild("001_HouseDoors")
                    if doors and doors:FindFirstChild("HouseDoorFront") then
                        for _, Base in ipairs(doors.HouseDoorFront:GetChildren()) do
                            if Base:IsA("BasePart") then
                                Base.CanCollide = not Value
                            end
                        end
                    end
                end
            end)
        end
    })
end)

-- Toggle para tocar campainha
pcall(function()
    CasasTab:AddToggle({
        Name = "Tocar Campainha",
        Description = "",
        Default = false,
        Callback = function(Value)
            getgenv().ChaosHubAutoSpawnDoorbellValue = Value
            spawn(function()
                while getgenv().ChaosHubAutoSpawnDoorbellValue do
                    local House = workspace["001_Lots"]:FindFirstChild(tostring(SelectHouse))
                    if House and House:FindFirstChild("HousePickedByPlayer") then
                        local doorbell = House.HousePickedByPlayer.HouseModel:FindFirstChild("001_DoorBell")
                        if doorbell and doorbell:FindFirstChild("TouchBell") then
                            pcall(function()
                                fireclickdetector(doorbell.TouchBell.ClickDetector)
                            end)
                        end
                    end
                    task.wait(0.5)
                end
            end)
        end
    })
end)

-- Toggle para bater na porta
pcall(function()
    CasasTab:AddToggle({
        Name = "Bater na Porta",
        Description = "",
        Default = false,
        Callback = function(Value)
            getgenv().ChaosHubAutoSpawnDoorValue = Value
            spawn(function()
                while getgenv().ChaosHubAutoSpawnDoorValue do
                    local House = workspace["001_Lots"]:FindFirstChild(tostring(SelectHouse))
                    if House and House:FindFirstChild("HousePickedByPlayer") then
                        local doors = House.HousePickedByPlayer.HouseModel:FindFirstChild("001_HouseDoors")
                        if doors and doors:FindFirstChild("HouseDoorFront") and doors.HouseDoorFront:FindFirstChild("Knock") then
                            pcall(function()
                                fireclickdetector(doors.HouseDoorFront.Knock.TouchBell.ClickDetector)
                            end)
                        end
                    end
                    task.wait(0.5)
                end
            end)
        end
    })
end)

    

-- --- ABA: CARRO ---

local TabCar = Window:MakeTab({"Carro", "car"}) 

local Section = TabCar:AddSection({"Veículos"})
TabCar:AddTextBox({Name = "Velocidade do Carro", Callback = function(t)
    local car = vu9.Vehicles:FindFirstChild(vu7.Name .. "Car")
    if car then for _, s in pairs(car:GetDescendants()) do if s:IsA("VehicleSeat") and s:FindFirstChild("TopSpeed") then s.TopSpeed.Value = tonumber(t) or 100 end end end
end})
TabCar:AddButton({Name = "Bring All Cars (Trazer Carros)", Callback = function()
    local char = vu7.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root then
        for _, v in next, vu9.Vehicles:GetChildren() do
            pcall(function()
                v:SetPrimaryPartCFrame(root.CFrame)
            end)
        end
    end
end})

TabCar:AddButton({Name = "🚀 Fly Car (Voo de Carro)", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fly-car-44742"))() end})

-- --- ABA: TELEPORTES ---
local TabTP = Window:MakeTab({"Teleportes", "map"}) 
local Section = TabTP:AddSection({"TELEPORTAR PARA..."})
TabTP:AddButton({ Name = "Ir para o Spawn", Callback = function() TeleportTo(CFrame.new(0, 15, 0)) end })
TabTP:AddButton({ Name = "Ir para o Motel", Callback = function() TeleportTo(CFrame.new(526, 16, -266)) end })
TabTP:AddButton({ Name = "Ir para a Escola", Callback = function() TeleportTo(CFrame.new(-340, 16, 260)) end })
TabTP:AddButton({ Name = "Campo de Futebol", Callback = function() TeleportTo(CFrame.new(-470, 3, 366)) end })   
TabTP:AddButton({ Name = "Tp para o sofá", Callback = function() TeleportTo(CFrame.new(-86, 19, -129)) end}) 
TabTP:AddButton({ Name = "Arco 1", Callback = function() TeleportTo(CFrame.new(623, 134, -60)) end })   
TabTP:AddButton({ Name = "Banco Secreto", Callback = function() TeleportTo(CFrame.new(5, -51, 223)) end }) 
TabTP:AddButton({ Name = "Fabrica", Callback = function() TeleportTo(CFrame.new(501, 3, 403)) end })   
TabTP:AddButton({ Name = "Tp para Montanha", Callback = function() TeleportTo(CFrame.new(-671, 250, 763)) end }) 
TabTP:AddButton({ Name = "Tp para o Lago", Callback = function() TeleportTo(CFrame.new(-222, 3, 1062)) end })   
TabTP:AddButton({ Name = "Agency Secret", Callback = function() TeleportTo(CFrame.new(653, -137, 503)) end })
-- --- ABA: EXTRAS E FE ---  
local TabFE = Window:MakeTab({"FE Scripts", "Script"})
local Section = TabFE:AddSection({"Fantasia FE"})
TabFE:AddButton({Name = "Ativar Goner", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Dboas123432sx/personagem/refs/heads/main/goner"))() end})
TabFE:AddButton({Name = "Ativar Flash", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Dboas123432sx/personagem/refs/heads/main/Flash%20original"))() end})
-- --- ABA: EXTRAS ---
local TabExtras = Window:MakeTab({"Extras", "Flash"})
local Section = TabExtras:AddSection({"Extras"})
TabExtras:AddSection({"Click Teleporte Tool"})
TabExtras:AddButton({Name = "Click TP Tool", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Teleport-Tools-34066"))() end})
 
TabExtras:AddSection({"UNIVERSAL COMMADS ADMIN🔨"})
TabExtras:AddButton({Name = "Nameless Admin", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Nameless-admin-Universal-73082"))() end}) 
TabExtras:AddButton({Name = "Infinite Yield", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Infinite-Yeild-93384"))() end})

-- --- LOOPS DE SISTEMA ---
local hue = 0
vu10.Heartbeat:Connect(function()
    hue = hue + 0.01 if hue > 1 then hue = 0 end
    local color = Color3.fromHSV(hue, 1, 1)
    
    if _G.RGBName then vu8.RE["1RPNam1eColo1r"]:FireServer("PickingRPNameColor", color) end
    if _G.RainbowBio then vu8.RE["1RPNam1eColo1r"]:FireServer("PickingRPBioColor", color) end
    
    local char = vu7.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if _G.AntiSit and hum then hum.Sit = false end
        
        local root = char:FindFirstChild("HumanoidRootPart")
        if _G.AntiVoid and root then
            local floor = vu9:FindFirstChild("AntiVoidFloor") or Instance.new("Part", vu9)
            floor.Name = "AntiVoidFloor"
            floor.Size = Vector3.new(2048, 2, 2048)
            floor.Anchored = true
            floor.Transparency = 1
            floor.Position = Vector3.new(root.Position.X, -490, root.Position.Z)
        elseif not _G.AntiVoid and vu9:FindFirstChild("AntiVoidFloor") then
            vu9.AntiVoidFloor:Destroy()
        end
    end
    if viewEnabled then updateCamera() end
end)

local TrollTab = Window:MakeTab({"Controls", "settings"})
Window:SelectTab(TabTroll)

local Section = TrollTab:AddSection({"Funções"})
TrollTab:AddSection({ "Puxar Parts" })
TrollTab:AddButton({
    Name = "Puxar Parts",
    Description = " Ativando isso você puxa Parts até o seu personagem",
    Callback = function()
        local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")

local angle = 1
local radius = 10
local blackHoleActive = false

local function setupPlayer()
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    local Folder = Instance.new("Folder", Workspace)
    local Part = Instance.new("Part", Folder)
    local Attachment1 = Instance.new("Attachment", Part)
    Part.Anchored = true
    Part.CanCollide = false
    Part.Transparency = 1

    return humanoidRootPart, Attachment1
end

local humanoidRootPart, Attachment1 = setupPlayer()

if not getgenv().Network then
    getgenv().Network = {
        BaseParts = {},
        Velocity = Vector3.new(100, 100, 100)
    }

    Network.RetainPart = function(part)
        if typeof(part) == "Instance" and part:IsA("BasePart") and part:IsDescendantOf(Workspace) then
            table.insert(Network.BaseParts, part)
            part.CustomPhysicalProperties = PhysicalProperties.new(80, 0, 0, 0, 0)
            part.CanCollide = false
        end
    end

    local function EnablePartControl()
        LocalPlayer.ReplicationFocus = Workspace
        RunService.Heartbeat:Connect(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            for _, part in pairs(Network.BaseParts) do
                if part:IsDescendantOf(Workspace) then
                    part.Velocity = Network.Velocity
                end
            end
        end)
    end

    EnablePartControl()
end

local function ForcePart(v)
    if v:IsA("Part") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
        for _, x in next, v:GetChildren() do
            if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then
                x:Destroy()
            end
        end
        if v:FindFirstChild("Attachment") then
            v:FindFirstChild("Attachment"):Destroy()
        end
        if v:FindFirstChild("AlignPosition") then
            v:FindFirstChild("AlignPosition"):Destroy()
        end
        if v:FindFirstChild("Torque") then
            v:FindFirstChild("Torque"):Destroy()
        end
        v.CanCollide = false
        
        local Torque = Instance.new("Torque", v)
        Torque.Torque = Vector3.new(100000000, 100000000, 100000000)
        local AlignPosition = Instance.new("AlignPosition", v)
        local Attachment2 = Instance.new("Attachment", v)
        Torque.Attachment0 = Attachment2
        AlignPosition.MaxForce = math.huge
        AlignPosition.MaxVelocity = math.huge
        AlignPosition.Responsiveness = 500
        AlignPosition.Attachment0 = Attachment2
        AlignPosition.Attachment1 = Attachment1
    end
end

local function toggleBlackHole()
    blackHoleActive = not blackHoleActive
    if blackHoleActive then
        for _, v in next, Workspace:GetDescendants() do
            ForcePart(v)
        end

        Workspace.DescendantAdded:Connect(function(v)
            if blackHoleActive then
                ForcePart(v)
            end
        end)

        spawn(function()
            while blackHoleActive and RunService.RenderStepped:Wait() do
                angle = angle + math.rad(2)

                local offsetX = math.cos(angle) * radius
                local offsetZ = math.sin(angle) * radius

                Attachment1.WorldCFrame = humanoidRootPart.CFrame * CFrame.new(offsetX, 0, offsetZ)
            end
        end)
    else
        Attachment1.WorldCFrame = CFrame.new(0, -1000, 0)
    end
end

LocalPlayer.CharacterAdded:Connect(function()
    humanoidRootPart, Attachment1 = setupPlayer()
    if blackHoleActive then
        toggleBlackHole()
    end
end)

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/miroeramaa/TurtleLib/main/TurtleUiLib.lua"))()
local window = library:Window("Projeto Darkzin")

window:Slider("Radius Blackhole",1,100,10, function(Value)
   radius = Value
end)

window:Toggle("Blackhole", true, function(Value)
       if Value then
            toggleBlackHole()
        else
            blackHoleActive = false
        end
end)

spawn(function()
    while true do
        RunService.RenderStepped:Wait()
        if blackHoleActive then
            angle = angle + math.rad(angleSpeed)
        end
    end
end)

toggleBlackHole()
    end
})


TrollTab:AddSection({ "Black Hole🕳" })
TrollTab:AddButton({
    Name = "Black Hole",
    Description = "Para usar, escreva o nome do alvo que deseja . aperte no botão de bring",
    Callback = function()
        -- Gui to Lua
-- Version: 3.2

-- Instances:

local Gui = Instance.new("ScreenGui")
local Main = Instance.new("Frame")
local Box = Instance.new("TextBox")
local UITextSizeConstraint = Instance.new("UITextSizeConstraint")
local Label = Instance.new("TextLabel")
local UITextSizeConstraint_2 = Instance.new("UITextSizeConstraint")
local Button = Instance.new("TextButton")
local UITextSizeConstraint_3 = Instance.new("UITextSizeConstraint")

--Properties:

Gui.Name = "Gui"
Gui.Parent = gethui()
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Main.Name = "Main"
Main.Parent = Gui
Main.BackgroundColor3 = Color3.fromRGB(75, 75, 75)
Main.BorderColor3 = Color3.fromRGB(0, 0, 0)
Main.BorderSizePixel = 0
Main.Position = UDim2.new(0.335954279, 0, 0.542361975, 0)
Main.Size = UDim2.new(0.240350261, 0, 0.166880623, 0)
Main.Active = true
Main.Draggable = true

Box.Name = "Box"
Box.Parent = Main
Box.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Box.BorderColor3 = Color3.fromRGB(0, 0, 0)
Box.BorderSizePixel = 0
Box.Position = UDim2.new(0.0980926454, 0, 0.218712583, 0)
Box.Size = UDim2.new(0.801089942, 0, 0.364963502, 0)
Box.FontFace = Font.new("rbxasset://fonts/families/SourceSansSemibold.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Box.PlaceholderText = "Name player"
Box.Text = "DARK X HUB "
Box.TextColor3 = Color3.fromRGB(255, 255, 255)
Box.TextScaled = true
Box.TextSize = 31.000
Box.TextWrapped = true

UITextSizeConstraint.Parent = Box
UITextSizeConstraint.MaxTextSize = 31

Label.Name = "Label"
Label.Parent = Main
Label.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Label.BorderColor3 = Color3.fromRGB(0, 0, 0)
Label.BorderSizePixel = 0
Label.Size = UDim2.new(1, 0, 0.160583943, 0)
Label.FontFace = Font.new("rbxasset://fonts/families/Nunito.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Label.Text = "Black Hole | Deve By: Carlolx777"
Label.TextColor3 = Color3.fromRGB(255, 255, 255)
Label.TextScaled = true
Label.TextSize = 14.000
Label.TextWrapped = true

UITextSizeConstraint_2.Parent = Label
UITextSizeConstraint_2.MaxTextSize = 21

Button.Name = "Button"
Button.Parent = Main
Button.BackgroundColor3 = Color3.fromRGB(95, 95, 95)
Button.BorderColor3 = Color3.fromRGB(0, 0, 0)
Button.BorderSizePixel = 0
Button.Position = UDim2.new(0.183284417, 0, 0.656760991, 0)
Button.Size = UDim2.new(0.629427791, 0, 0.277372271, 0)
Button.Font = Enum.Font.Nunito
Button.Text = "Bring | Off"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextScaled = true
Button.TextSize = 28.000
Button.TextWrapped = true

UITextSizeConstraint_3.Parent = Button
UITextSizeConstraint_3.MaxTextSize = 28

-- Scripts:

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local character
local humanoidRootPart

mainStatus = true
UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
	if input.KeyCode == Enum.KeyCode.RightControl and not gameProcessedEvent then
		mainStatus = not mainStatus
		Main.Visible = mainStatus
	end
end)

local Folder = Instance.new("Folder", Workspace)
local Part = Instance.new("Part", Folder)
local Attachment1 = Instance.new("Attachment", Part)
Part.Anchored = true
Part.CanCollide = false
Part.Transparency = 1

if not getgenv().Network then
	getgenv().Network = {
		BaseParts = {},
		Velocity = Vector3.new(10000, 10000, 10000)
	}

	Network.RetainPart = function(Part)
		if Part:IsA("BasePart") and Part:IsDescendantOf(Workspace) then
			table.insert(Network.BaseParts, Part)
			Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
			Part.CanCollide = false
		end
	end

	local function EnablePartControl()
		LocalPlayer.ReplicationFocus = Workspace
		RunService.Heartbeat:Connect(function()
			sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
			for _, Part in pairs(Network.BaseParts) do
				if Part:IsDescendantOf(Workspace) then
					Part.Velocity = Network.Velocity
				end
			end
		end)
	end

	EnablePartControl()
end

local function ForcePart(v)
	if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
		for _, x in ipairs(v:GetChildren()) do
			if x:IsA("BodyMover") or x:IsA("RocketPropulsion") then
				x:Destroy()
			end
		end
		if v:FindFirstChild("Attachment") then
			v:FindFirstChild("Attachment"):Destroy()
		end
		if v:FindFirstChild("AlignPosition") then
			v:FindFirstChild("AlignPosition"):Destroy()
		end
		if v:FindFirstChild("Torque") then
			v:FindFirstChild("Torque"):Destroy()
		end
		v.CanCollide = false
		local Torque = Instance.new("Torque", v)
		Torque.Torque = Vector3.new(1000000000, 1000000000, 1000000000)
		local AlignPosition = Instance.new("AlignPosition", v)
		local Attachment2 = Instance.new("Attachment", v)
		Torque.Attachment0 = Attachment2
		AlignPosition.MaxForce = math.huge
		AlignPosition.MaxVelocity = math.huge
		AlignPosition.Responsiveness = 2000
		AlignPosition.Attachment0 = Attachment2
		AlignPosition.Attachment1 = Attachment1
	end
end

local blackHoleActive = false
local DescendantAddedConnection

local function toggleBlackHole()
	blackHoleActive = not blackHoleActive
	if blackHoleActive then
		Button.Text = "Bring | On"
		for _, v in ipairs(Workspace:GetDescendants()) do
			ForcePart(v)
		end

		DescendantAddedConnection = Workspace.DescendantAdded:Connect(function(v)
			if blackHoleActive then
				ForcePart(v)
			end
		end)

		spawn(function()
			while blackHoleActive and RunService.RenderStepped:Wait() do
				Attachment1.WorldCFrame = humanoidRootPart.CFrame
			end
		end)
	else
		Button.Text = "Bring | Off"
		if DescendantAddedConnection then
			DescendantAddedConnection:Disconnect()
		end
	end
end

local function getPlayer(name)
	local lowerName = string.lower(name)
	for _, p in pairs(Players:GetPlayers()) do
		local lowerPlayer = string.lower(p.Name)
		if string.find(lowerPlayer, lowerName) then
			return p
		elseif string.find(string.lower(p.DisplayName), lowerName) then
			return p
		end
	end
end

local player = nil

local function VDOYZQL_fake_script() -- Box.Script 
	local script = Instance.new('Script', Box)

	script.Parent.FocusLost:Connect(function(enterPressed)
		if enterPressed then
			player = getPlayer(Box.Text)
			if player then
				Box.Text = player.Name
				print("Player found:", player.Name)
			else
				print("Player not found")
			end
		end
	end)
end
coroutine.wrap(VDOYZQL_fake_script)()
local function JUBNQKI_fake_script() -- Button.Script 
	local script = Instance.new('Script', Button)

	script.Parent.MouseButton1Click:Connect(function()
		if player then
			character = player.Character or player.CharacterAdded:Wait()
			humanoidRootPart = character:WaitForChild("HumanoidRootPart")
			toggleBlackHole()
		else
			print("Player is not selected")
		end
	end)
end
coroutine.wrap(JUBNQKI_fake_script)()
    end
})

-- --- ESP SYSTEM CORREÇÃO ---
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local billboardGuis = {}
local connections = {}
local espEnabled = false
local selectedColor = "RGB"

Tab2 = Window:MakeTab({"ESP", "eye"}) -- Criando aba ESP se não existir
Tab2:AddDropdown({
    Name = "Cor do ESP",
    Default = "RGB",
    Options = {"RGB", "Branco", "Preto", "Vermelho", "Verde", "Azul", "Amarelo", "Rosa", "Roxo"},
    Callback = function(value) selectedColor = value end
})

local function getESPColor()
    if selectedColor == "RGB" then return Color3.fromHSV((tick() % 5) / 5, 1, 1) end
    local cols = {Branco=Color3.new(1,1,1), Preto=Color3.new(0,0,0), Vermelho=Color3.new(1,0,0), Verde=Color3.new(0,1,0), Azul=Color3.new(0,0.6,1), Amarelo=Color3.new(1,1,0), Rosa=Color3.new(1,0.4,0.7), Roxo=Color3.new(0.5,0,1)}
    return cols[selectedColor] or Color3.new(1,1,1)
end

local function updateESP(player)
    if player == Players.LocalPlayer or not espEnabled then return end
    local char = player.Character
    if char and char:FindFirstChild("Head") then
        if billboardGuis[player] then billboardGuis[player]:Destroy() end
        local bill = Instance.new("BillboardGui", char.Head)
        bill.Size = UDim2.new(0,200,0,50)
        bill.AlwaysOnTop = true
        bill.StudsOffset = Vector3.new(0,3,0)
        local lab = Instance.new("TextLabel", bill)
        lab.Size = UDim2.new(1,0,1,0)
        lab.BackgroundTransparency = 1
        lab.Text = player.Name .. " | " .. player.AccountAge .. "d"
        lab.TextColor3 = getESPColor()
        lab.Font = Enum.Font.SourceSansBold
        lab.TextSize = 14
        billboardGuis[player] = bill
    end
end

-- CORREÇÃO DO TOGGLE ESP (SINTAXE DA REDZLIB)
Tab2:AddToggle({
    Name = "ESP Ativado",
    Description = "Mostra nome e idade da conta dos jogadores.",
    Default = false,
    Callback = function(value)
        espEnabled = value
        if not value then
            for _, v in pairs(billboardGuis) do v:Destroy() end
            billboardGuis = {}
        end
    end
})

RunService.Heartbeat:Connect(function()
    if espEnabled then
        for _, p in pairs(Players:GetPlayers()) do updateESP(p) end
    end
end)
        
 local Tab3  = Window:MakeTab({"Avatar", "shirt"})

local Section = Tab3:AddSection({"Roupas 3D"})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local AvatarManager = {}
AvatarManager.ReplicatedStorage = ReplicatedStorage

function AvatarManager:MostrarNotificacao(mensagem)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Aviso",
            Text = mensagem,
            Duration = 5
        })
    end)
end

AvatarManager.Avatares = {
    { Nome = "Gato de Manga", ID = 10265120934},
    { Nome = "Tung Saur", ID = 117098257036480 },
    { Nome = "Tralaleiro", ID = 99459753608381 },
    { Nome = "Monstro S.A", ID = 123609977175226 },
    { Nome = "Trenzinho", ID = 80468697076178 },
    { Nome = "Dino", ID = 11941741105 },
    { Nome = "Pou idoso", ID = 15742966010 },
    { Nome = "Coco/boxt@", ID = 77013984520332 },
    { Nome = "Coelho", ID = 71797333686800 },
    { Nome = "Hipopótamo", ID = 73215892129281 },
    { Nome = "Ratatui", ID = 108557570415453 },
    { Nome = "Galinha", ID = 71251793812515 },
    { Nome = "Pepa pig", ID = 92979204778377 },
    { Nome = "pinguin", ID = 94944293759578 },
    { Nome = "Sid", ID = 87442757321244 },
    { Nome = "puga grande", ID = 111436158728716 },
    { Nome = "SHREK AMALDIÇOADO", ID = 120960401202173 },
    { Nome = "mosquito grande", ID = 108052868536435 },
    { Nome = "Noob Invertido", ID = 106596990206151 },
    { Nome = "Pato(a)", ID = 135132836238349 },
    { Nome = "Cachorro Chihuahua", ID = 18656467256 },
    { Nome = "Gato sla", ID = 18994959003 },
    { Nome = "Gato fei", ID = 77506186615650 },
    { Nome = "Inpostor", ID = 18234669337 },
    { Nome = "Simon amarelo", ID = 75183593514657 },
    { Nome = "Simon azul", ID = 76155710249925 }
}

function AvatarManager:GetAvatarNames()
    local nomes = {}
    for _, avatar in ipairs(self.Avatares) do
        table.insert(nomes, avatar.Nome)
    end
    return nomes
end

function AvatarManager:EquiparAvatar(avatarName)
    for _, avatar in ipairs(self.Avatares) do
        if avatar.Nome == avatarName then
            local args = { avatar.ID }
            local success, result = pcall(function()
                return self.ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Wear"):InvokeServer(unpack(args))
            end)
            if success then
                self:MostrarNotificacao("Avatar " .. avatarName .. " equipado com sucesso!")
            else
                self:MostrarNotificacao("Falha ao equipar o avatar " .. avatarName .. "!")
            end
            return
        end
    end
    self:MostrarNotificacao("Avatar " .. avatarName .. " não encontrado!")
end

local AvatarDropdown = Tab3:AddDropdown({
    Name = "Assesórios 3D",
    Description = "Selecione para equipar",
    Default = nil,
    Options = AvatarManager:GetAvatarNames(),
    Callback = function(avatarSelecionado)
        _G.SelectedAvatar = avatarSelecionado
    end
})

Tab3:AddButton({
    Name = "Equipar",
    Description = "Equipar selecionado",
    Callback = function()
        if not _G.SelectedAvatar or _G.SelectedAvatar == "" then
            AvatarManager:MostrarNotificacao("Nenhum avatar selecionado!")
            return
        end
        AvatarManager:EquiparAvatar(_G.SelectedAvatar)
    end
})
local Section = Tab3:AddSection({"Avatar Editor"})

Tab3:AddParagraph({
    Title = "Aviso: vai resetar seu avatar",
    Content = ""
})

Tab3:AddButton({
    Name = "Mini REPO",
    Callback = function()
        local args = {{
            117101023704825,125767940563838,137301494386930,87357384184710,133391239416999,111818794467824
        }}
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
    end
})

Tab3:AddButton({
    Name = "Mini Garanhão",
    Callback = function()
        local args = {{
            124355047456535,120507500641962,82273782655463,113625313757230,109182039511426,0
        }}
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
    end
})

Tab3:AddButton({
    Name = "Stick",
    Callback = function()
        local args = {{
            14731384498,14731377938,14731377894,14731377875,14731377941,14731382899
        }}
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
    end
})

Tab3:AddButton({
    Name = "Chunky-Bug",
    Callback = function()
        local args = {{
            15527827600,15527827578,15527831669,15527836067,15527827184,15527827599
        }}
        game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("ChangeCharacterBody"):InvokeServer(unpack(args))
    end
})
         
local TabSpam = Window:MakeTab({"Spam", "home"})

TabSpam:AddSection({("Spam Varios Jatos <BETA>")})

local JetSystem = {
    spawnActive = false,
    spawnLoop = nil,
    originalPosition = nil,
    teleportPosition = CFrame.new(488.179443359375, -20.3643856048584, 234.5631866455078),
    destinations = {
        CFrame.new(-28.919492721557617, 2.9356155395507812, -9.202991485595703),
        CFrame.new(14.021419525146484, 2.9356155395507812, 2.162062168121338),
        CFrame.new(15.570757865905762, 2.935615301132202, 37.51002502441406),
        CFrame.new(-23.0268611907959, 2.935615062713623, 60.1992073059082),
        CFrame.new(-45.52577209472656, 2.835615634918213, 36.096221923828125)
    }
}

local function getJetComponents()
    local airport = workspace:FindFirstChild("WorkspaceCom")
    if not airport then return nil, nil, nil end
    
    airport = airport:FindFirstChild("001_Airport")
    if not airport then return nil, nil, nil end
    
    local hanger = airport:FindFirstChild("AirportHanger")
    if not hanger then return nil, nil, nil end
    
    local buttonPart = hanger:FindFirstChild("001_JetCloneButton")
    local storage = hanger:FindFirstChild("001_JetStorage")
    
    if not buttonPart or not storage then return nil, nil, nil end
    
    local button = buttonPart:FindFirstChild("Button")
    if not button then return nil, nil, nil end
    
    local detector = button:FindFirstChild("ClickDetector")
    
    return detector, storage, button
end

local function findNewJet(usedJets, storage)
    for _, jet in ipairs(storage:GetChildren()) do
        if jet:IsA("Model") and jet:FindFirstChild("NoUse") and not table.find(usedJets, jet) then
            return jet
        end
    end
    return nil
end

local function startJetSpam()
    local humanoidRootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end

    local detector, storage = getJetComponents()
    if not detector or not storage then return end

    JetSystem.originalPosition = humanoidRootPart.CFrame
    humanoidRootPart.CFrame = JetSystem.teleportPosition
    JetSystem.spawnActive = true

    task.spawn(function()
        local destinationIndex = 1
        local usedJets = {}

        while JetSystem.spawnActive do
            if detector then
                fireclickdetector(detector)
            end

            task.wait(Config.jetSpawnDelay)

            local newJet = findNewJet(usedJets, storage)
            if newJet then
                local height = (#usedJets + 1) * 6
                local baseDestination = JetSystem.destinations[destinationIndex] or JetSystem.destinations[1]
                local target = baseDestination + Vector3.new(0, height, 0)

                local primaryPart = newJet:FindFirstChild("NoUse") or newJet:FindFirstChildWhichIsA("BasePart")
                if primaryPart then
                    newJet.PrimaryPart = primaryPart
                    newJet:SetPrimaryPartCFrame(target)
                    table.insert(usedJets, newJet)
                end
            end

            destinationIndex = (destinationIndex % #JetSystem.destinations) + 1
            task.wait(0.5)
        end
    end)
end

local function stopJetSpam()
    JetSystem.spawnActive = false
    if JetSystem.originalPosition then
        local humanoidRootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if humanoidRootPart then
            humanoidRootPart.CFrame = JetSystem.originalPosition
        end
    end
end

TabSpam:AddButton({
  Name = ("Como funciona?"),
    function()
        Window:Dialog({
            Title = ("Funções Do jatos"),
            Text = ("• Para funcionar precisa de GamePass <Premium> é versão Beta. Pode conter erros. Se demorar, recomendo ativar e desativar!"),
            Options = {
                {"Fechar", function() end}
            }
        })
    end
})
TabSpam:AddButton({
 Name = "Spamar Jatos",
    function()
        Window:Dialog({
            Title = ("Como Funciona?"),
            Text = ("O spam começa em até 20 segundos. Não é visual. Após usar, não toque em nada, apenas no script."),
            Options = {
                {"Começar Spam", function()
                    if not JetSystem.spawnActive then
                        startJetSpam()
                    end
                end},
                {"Fechar", function() end}
            }
        })
    end
})

TabSpam:AddButton({
   Name = ("Parar Spam"),
    function()
        stopJetSpam()
    end
})

TabSpam:AddSection({("Auto Spamar Jatos <Jogador>")})

local TargetJetSystem = {
    flingActive = false,
    targetPlayer = nil,
    cameraConnection = nil
}

local function getPlayerList()
    local playerList = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(playerList, player.Name)
        end
    end
    return playerList
end

local function startTargetJetSpam()
    TargetJetSystem.flingActive = true

    local humanoidRootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if humanoidRootPart then 
        humanoidRootPart.CFrame = JetSystem.teleportPosition 
    end

    task.spawn(function()
        local usedJets = {}
        local detector, storage = getJetComponents()

        while TargetJetSystem.flingActive do
            if detector then
                fireclickdetector(detector)
            end

            task.wait(Config.jetSpawnDelay)

            local jet = findNewJet(usedJets, storage)
            local targetPlayer = Players:FindFirstChild(TargetJetSystem.targetPlayer)

            if jet and targetPlayer and targetPlayer.Character then
                local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                if targetHRP then
                    local primaryPart = jet:FindFirstChild("NoUse") or jet:FindFirstChildWhichIsA("BasePart")
                    if primaryPart then
                        jet.PrimaryPart = primaryPart
                        local offset = Vector3.new(
                            math.random(-3, 3),
                            math.random(4, 8),
                            math.random(-3, 3)
                        )
                        local destination = targetHRP.Position + offset
                        jet:SetPrimaryPartCFrame(CFrame.new(destination))
                        table.insert(usedJets, jet)
                    end
                end
            end

            task.wait(Config.jetMoveDelay)
        end
    end)
end

local function stopTargetJetSpam()
    TargetJetSystem.flingActive = false
end

local function updateCameraView(enabled)
    if enabled then
        local targetPlayer = Players:FindFirstChild(TargetJetSystem.targetPlayer)
        if targetPlayer and targetPlayer.Character then
            local humanoid = targetPlayer.Character:FindFirstChild("Humanoid")
            if humanoid then
                Camera.CameraSubject = humanoid
            end
        end
    else
        if LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
            if humanoid then
                Camera.CameraSubject = humanoid
            end
        end
    end
end

local playerDropdown = TabSpam:AddDropdown({
    Name = ("Selecionar Jogador"),
    Description = ("Selecione uma pessoa para mandar os jatos"),
    Options = getPlayerList(),
    Default = "Pessoa",
    Callback = function(value)
        TargetJetSystem.targetPlayer = value
    end
})

TabSpam:AddToggle({
    Name = ("Visualizar Jogador"),
    Description = ("Mesmo de longe você consegue ver"),
    Default = false,
    Callback = function(value)
        if value then
            if not TargetJetSystem.cameraConnection then
                TargetJetSystem.cameraConnection = RunService.Heartbeat:Connect(function()
                    updateCameraView(true)
                end)
            end
        else
            if TargetJetSystem.cameraConnection then
                TargetJetSystem.cameraConnection:Disconnect()
                TargetJetSystem.cameraConnection = nil
            end
            updateCameraView(false)
        end
    end
})

TabSpam:AddButton({
   Name = ("Atualizar Lista"),
    function()
        playerDropdown:Set(getPlayerList())
    end
})

TabSpam:AddToggle({
    Name = ("Auto Spamer"),
    Description = ("Spam de jets constante ao jogador selecionado"),
    Default = false,
    Callback = function(value)
        if value then
            startTargetJetSpam()
        else
            stopTargetJetSpam()
        end
    end
})
